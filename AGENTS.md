# AGENTS.md

このリポジトリで作業するAI（Codex など）への指示。
**ルールの正本は `Documents/README.md`。このファイルはその入口。**

---

## 起動したら、まずこの順で行う

### 1. `Documents/README.md` を読む

作業内容にかかわらず、**必ず最初に読む**こと。
プロジェクトの方針・禁止事項・Gitの運用は、すべてそこに書かれている。

### 2. 相手が誰かを確認する

`Documents/README.md` の「メンバー構成」に相手の名前が無い場合、**作業を始める前に尋ねる**。

> 作業の前に確認させてください。お名前と、役職（プログラマー／デザイナー／プランナー）を教えてください。

聞き取ったら：

- `Documents/README.md` の「メンバー構成」に追記する
- `Documents/README.md` の「変更記録」に1行足す
- **記憶機能があるAIは、そこにも保存する**（次回から聞き直さないため）

すでに名前が載っている人には、尋ね直さない。

### 3. 初めての人にだけ説明する

「このプロジェクトでAIを使うのは初めてですか？」と尋ね、**初めての場合のみ**
`Documents/README.md` の「説明」にある4項目を案内する。**毎回説明しないこと。**

---

## 分からないことがあったら

1. **まず `Documents/` の中を探す**（README / 機能ドキュメント / 制作ログ / リスクリスト / 使用素材とライセンス）。
2. 見つかったら、その内容をユーザーに分かりやすく伝える。
3. ドキュメントに書かれておらず、**AIでは判断できない方針の問題**だった場合は、
   勝手に決めずに、次の窓口へ確認するようユーザーに案内する。

| 相談したい内容 | 相談先 |
| --- | --- |
| Git・GitHubの操作、AI（Codex / Claude Code）の使い方、技術的な方針 | **大槻 海斗**（GitHub・AI管理担当） |
| 企画・仕様の方針 | **サブリーダー**（プランナー） |
| デザイン・グラフィックの方針 | **プロジェクトリーダー**（デザイナー） |

- 連絡手段は**チームDiscord**。
- 急ぎでない質問は `Documents/質問リスト.md` に書き足しておけばよい。AIが代わりに追記してもよい。

---

## 絶対に守ること

- **Unityのバージョンは `6000.3.14f1` から変更しない。** アップグレードの確認が出ても承諾しない。
- **`Mirai01/Assets/` 以下を、OSのファイル操作で移動・リネーム・削除しない。**
  `.meta` ファイルとの対応が壊れ、シーンやプレハブの参照が外れる。必要ならUnityエディタ上での手順を案内する。
- **`Assets/` 以下に勝手に新しいフォルダを作らない。** 置き場所を確認してから作業する。
- シーン（`.unity`）とプレハブ（`.prefab`）は自動マージできない。
  編集した場合は**必ずその旨を報告する**。
- **既存のシーンに機能を足さない。新しい機能は、その機能専用のシーンを新しく作る。**
  そのシーンを再生するだけで機能を検証できる状態にし、完成したらプレハブ化を目指す。
  詳しくは `Documents/シーンとプレハブの作り方.md` を読むこと。
- APIキー・パスワード・個人情報をコードやドキュメントに書かない。
- コンフリクトは**自力で解決せず**、どのファイルのどこが衝突したかを説明して指示を仰ぐ。

---

## Git

> **【暫定：2026年8月30日時点】**
> `develop` ブランチはまだ無く、作業しているのは大槻 海斗のみ。
> ブランチ運用の説明が全員に行われるまでは、`main` で直接作業してよい。
> 運用が始まったらこの枠を削除し、`Documents/README.md` のGit Flow運用に従う。

- 変更したらコミットし、**プッシュまで行う**。コミットメッセージには何をしたかを書く。
- `main` へのマージは、実行前に**大槻 海斗の確認**を取る。
- 「コミットしないで」等の個別の指示があれば、**そちらを優先する**。

---

## 実装したら記録する

- `Documents/` に新しいファイルを作るときは、**中身が一目で分かる日本語のファイル名**にする。英語の略語は使わない。
- 機能の詳細 → `Documents/機能ドキュメント/` に**1機能1ファイル**（`_テンプレート.md` をコピーして使う）
- 1行の記録 → `Documents/制作ログ.md`（最新が上）
- 安全性に不安が残る実装 → `Documents/リスクリスト.md`
- 外部素材を追加 → `Documents/使用素材とライセンス.md`（**入手した時点で書く**）

説明は専門用語を避け、**誰が読んでも分かる言葉**で書くこと。

---

## ドキュメントの扱い

`Documents/` 配下は**主にAIが読み書きする場所**である。人間が目を通すのはたまにでよい。
だからAIは、この場所を**自分たちのために自由に使ってよい。**

### 自動で変更してよい

- `Documents/` 配下のドキュメントは、**いちいち確認を取らずに変更してよい**。
- 変更したら、そのファイルの**一番下の変更ログに1行足す**。
- `Mirai01/Assets/` 以下は対象外。指示された範囲でのみ編集する。

### 自由に書き残してよい

次のようなことは、**許可を取らずにやってよい。**

- 作業していて「このサイクルはこう変えたほうがいい」と気づいたことを書き残す
- 失敗した手順を「こうやって失敗したので気をつけて」と、**後から来る別のAIに向けて**残す
- うまくいったやり方、詰まりやすい場所、環境の癖などをメモする
- **AI同士が読み書きするためのドキュメントを新しく作る**
- 既存のドキュメントが使いにくければ、構成を作り変える

置き場として `Documents/AIの申し送り.md` を用意してある。
足りなければ**新しいファイルを作ってよい**。ファイル名は日本語で、中身が分かる名前にすること。

このプロジェクトでは、複数のAIが別々のPCで、別々のメンバーの隣で作業する。
**同じ失敗を別のAIが繰り返さないように残しておくこと**は、実装と同じくらい価値がある。

守るのは次の2つだけ。

- 変更したファイルの**変更ログに1行足す**
- `Documents/README.md` の**ルールに関わる部分**を変えたときは、変更ログに残すだけでなく、
  **チャットでも何を変えたか必ず伝える**（人間が把握できなくなるため）

### 変更があったら「みんなの作業状況」として伝える

作業を始めるとき（`git pull` のあとなど）に `Documents/` のファイルが**前回から変わっていた**場合、
それは**他のメンバーが作業した記録**である。作業に入る前に、内容を要約してユーザーに伝えること。

- 見る場所 … `制作ログ.md`（追加された機能）、`リスクリスト.md`（増えた不安箇所）、
  `質問リスト.md`（新しい質問と回答）、`機能ドキュメント/`（増えたファイル）、`AIの申し送り.md`（他のAIからの申し送り）
- 伝え方 … 「前回から、○○さんが△△を追加しています」のように、**誰が・何をしたか**が分かる短いまとめ
- 調べ方の例：

  ```bash
  git log --oneline -- Documents/
  ```

- これから触る予定のファイルやシーンが他の人の作業に含まれていた場合は、**必ず先にその旨を伝える**。

---

## `Auto:` 指示への対応

ユーザーの指示が **`Auto:` または `/Auto` で始まる場合**（例：`Auto: バグを探して` `/Auto 改善点を探して`）、
**自律作業モード**として扱う。

> `Auto:` が正式な書き方。`/Auto` は、ターミナルで動かすClaude Codeでのみ使える別名
> （`.claude/commands/Auto.md` に登録してある）。アプリ版のClaude CodeやCodexでは、
> `/` で始まる入力がコマンドとして処理されてしまい届かないため、**`Auto:` を使うよう案内すること。**終わりがはっきりしない作業を、途中で止まらずに進めるための仕組み。

### 1. 作業前に宣言する

作業に入る前に、次の2つを必ず伝える。

1. **指示の意図の推測** … 「○○と推測されるため、□□をします」
2. **継続基準** … どうなったら終了するか。ユーザーが指定していれば**必ずそちらを使う**。
   「何周したら」「何が無くなったら」のように、**AI自身で判定できる形**にすること。

> 例：「『バグを探して』は、動かなくなる箇所を先に潰しておきたいという意図と推測されるため、
> `Assets/` 以下のスクリプトを順に読み、見つけたバグを `Documents/リスクリスト.md` にまとめます。
> 継続基準は『未確認のスクリプトが無くなるまで』とします。」

### 2. 作業用のブランチを切る

この作業は**必ず新しいブランチを切って行う**。`main` や作業中のブランチの上で直接動かさない。

- 例：`auto/bug-check-0830`（`auto/内容-日付`）
- ブランチを切ったことも、その名前とあわせてユーザーに伝える。

### 3. 指示の意図を汲んで、成果を形に残す

言われたことをなぞるだけでなく、**その指示で本当に欲しかったもの**を考えて残すこと。

- 「バグを探して」 → 見つけたバグの**場所・再現条件・影響・直し方の案**を `Documents/リスクリスト.md` にまとめる
- 「改善点を探して」 → 改善案を `Documents/AIの申し送り.md` や機能ドキュメントにまとめる
- 適した置き場が無ければ、**新しいドキュメントを作ってよい**

### 4. こまめにコミットする（セーブ）

- ひと区切りつくたびにコミットする。**コミットはセーブポイント**である。
- 大きな変更に取りかかる前には、**必ず1回コミットしておく**。不具合が出てもすぐ戻れるようにするため。
- コミットメッセージには、何をしたかが分かる内容を書く。

### 5. 何をしたかを都度伝える

- ファイルを編集したら、**チャットで簡単に説明する**（1〜2行でよい）。
  例：「`PlayerMove.cs` に null チェックを追加しました。`リスクリスト.md` にも1件登録しています。」
- **確認は取らない。報告だけして次に進む。**

### 6. 迷っても止まらない

方針の決定が必要になったら `Documents/質問リスト.md` に質問として書き足し、
その項目だけ保留にして次へ進む。

### 7. 終了条件（いずれか一つ）

- ユーザーが停止を指示した
- 宣言した**継続基準を満たした**
- 使用できる**クレジット（利用上限）が尽きた**

### 8. 終了時

- **何周して・何を見つけて・何を直して・何を残したか**をまとめて報告する。
- 使った**ブランチ名**を伝え、マージするかどうかをユーザーに確認する。
- 次のAIに伝えることがあれば `Documents/AIの申し送り.md` に書き残す。

### 9. 自律作業モード中でも守ること

- 上の「絶対に守ること」「ファイル操作の禁止事項」はそのまま適用される。
- **`main` へのマージは行わない。**
- **他の人が編集中のファイルは変更しない。**
- ファイルを削除する場合は、**直前にコミットしてから**行う。
