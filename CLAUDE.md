# CLAUDE.md

このリポジトリのAI向けルールは `AGENTS.md` にまとめてある。
**`AGENTS.md` と `Documents/README.md` を必ず読んでから作業すること。**

@AGENTS.md

---

## Claude Code 固有の補足

- 聞き取ったメンバーの**名前と役職**は、`Documents/README.md` への追記に加えて
  **メモリにも保存する**こと。次回以降、聞き直さずに済むようにする。
- 自律作業モードの呼び出しは、**`Auto: バグを探して` のように `Auto:` で始まる形が正式**。
  `/Auto` はターミナル版でのみ動く別名なので、アプリ版で `/Auto` が効かないと言われたら `Auto:` を案内する。
- ファイルを変更したら、ユーザーが差分を確認できるよう**何を変えたかを簡潔に伝える**こと。
  Gitに不慣れなメンバーが多いため、「どのファイルが変わったか」を必ず言葉でも示す。
