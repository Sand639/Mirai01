# 未来創造点 出展作品プロジェクト（Mirai01）

| 項目 | 内容 |
| --- | --- |
| 製作者 | 大槻 海斗 |
| 作成日 | 2026年8月28日（金） |
| 最終更新日 | 2026年8月30日（日）　変更者：大槻 海斗 |
| リポジトリ | https://github.com/Sand639/Mirai01 |

> このドキュメントはプロジェクトの入口です。**参加したらまずこれを最後まで読むこと。**
> 分からない用語が出てきたら、リーダーかAI（Codex / Claude Code）に聞いて構いません。

---

## はじめに

このプロジェクトは、HAL東京のイベントである **未来創造点** に出展するゲームを制作するためのプロジェクトである。
制作は **Unity** を用いて行う。

### イベントテーマ

> **「100年後 ～未来を創造する～」**

企画・仕様はこのテーマに沿って考えること。

---

## 作品概要

| 項目 | 内容 |
| --- | --- |
| タイトル（仮） | 未定 |
| ジャンル | 未定 |
| コンセプト（一言で） | 未定 |
| プレイ人数／操作デバイス | 未定 |
| 対応プラットフォーム | 未定（PC想定） |

企画は現在未定のため、AIには実装だけでなく、**アイデア出し・仕様の整理・企画書の下書き**も手伝わせてよい。

### プレイ時間と展示の方針

本作はプレイ時間が長くなることを許容する。
ただし展示ブースでは長時間の占有ができないため、次の形をとる。

- **展示用ビルド** … チュートリアル、または体験版として短時間で遊び切れる範囲を切り出したもの
- **本編ビルド** … 別途ダウンロード可能な形で配布する

このため、**展示用ビルドと本編ビルドを切り替えられる作り**を前提に設計すること。
展示ブースには必ず担当者が在席するため、無人時の自動タイトル復帰は必須ではない。

> **実現方法（案・要決定）**
> Unityの **Scripting Define Symbols**（例：`EXHIBITION_BUILD`）でビルドを切り替える方式を想定している。
> シーンやプレハブを2種類に分けてしまうとマージが困難になるため、**同じシーンを条件分岐で切り替える**方針を推奨する。
> 実装担当が決まった時点で、この欄に決定内容を書くこと。

---

## スケジュール

| 項目 | 期日 |
| --- | --- |
| **マスターアップ** | **2026年12月18日（金）** |
| 機能追加を止める日（チーム内の目安） | 　月　日頃（**要決定**） |
| アルファ版・ベータ版 | 提出期限なし（チーム内の目標は必要に応じて設定） |

> マスターアップ直前の機能追加はバグの温床になる。
> 「機能追加を止める日」は、マスターアップの **2週間前程度** を目安に早めに決めておくこと。

---

## 開発環境

| 項目 | 内容 |
| --- | --- |
| Unity | **6000.3.14f1**（Unity 6系。全員このバージョンで統一） |
| レンダーパイプライン | URP（Universal Render Pipeline）17.3.0 |
| 入力 | Input System 1.19.0（新入力システム。旧 `Input.GetKey` 系は使わない） |
| エディタ（IDE） | 未定（Visual Studio / Rider どちらのパッケージも導入済み） |
| リポジトリ | https://github.com/Sand639/Mirai01 |
| 連絡手段 | 未定 |

> **Unityのバージョンは 6000.3.14f1 で固定する。**
> Unity Hubが「新しいバージョンで開きますか？」と聞いてきても **絶対にアップグレードしないこと。**
> 一度でも新しいバージョンで開くとプロジェクトファイルが書き換わり、他のメンバーが開けなくなる。
> バージョンを上げる場合は、リーダーが全員に告知したうえで一斉に行う。

---

## メンバー構成（10人）

### プログラマー（5人）

- 大槻 海斗

### デザイナー（3人）

- （未記入）

### プランナー（2人）

- （未記入）

> 自分の名前がまだ無い人は、AIに「READMEのメンバーに自分を追加して」と頼めば追記される。

---

## リポジトリ構成

```
Mirai01/                    ← リポジトリのルート
├── Documents/              ドキュメント（このREADMEがある場所）
│   ├── README.md           このファイル
│   ├── Features/           機能ごとの詳細ドキュメント（1機能1ファイル）
│   │   └── _TEMPLATE.md    新しい機能ドキュメントの雛形（コピーして使う）
│   ├── DevLog.md           制作ログ
│   ├── RiskList.md         リスクリスト
│   └── Credits.md          使用素材とライセンス
└── Mirai01/                Unityプロジェクト本体（Unity Hubではこのフォルダを開く）
    ├── Assets/             ゲーム本体（内部の構成は未定）
    ├── Packages/
    └── ProjectSettings/
```

> **各ドキュメントの役割**
>
> | ファイル | 何を書く場所か | 誰が書くか |
> | --- | --- | --- |
> | `Features/` | 機能ごとの詳細。1機能1ファイル。`_TEMPLATE.md` をコピーして作る | 実装した人／AI |
> | `DevLog.md` | 「誰が・いつ・何を追加したか」の1行記録 | 実装した人／AI |
> | `RiskList.md` | 動作優先で残した不安箇所。バグ調査時に最初に見る | 実装した人／AI |
> | `Credits.md` | 外部素材の名称・入手元・ライセンス。**入手した時点で書く** | 素材を入れた人 |
>
> `Mirai01/Assets/` の中の構成はまだ決まっていない。
> AIは `Assets/` 以下に新しいフォルダを勝手に作らず、**置き場所を確認してから**作業すること。

---

## 初回セットアップ手順

1. **Unity Hub** をインストールする。
2. Unity Hub から **Unity 6000.3.14f1** をインストールする。
   - モジュールは、作るプラットフォームに合わせて選ぶ（PC向けなら追加モジュールは基本不要）。
3. **Git** をインストールする（Windowsなら [Git for Windows](https://gitforwindows.org/)）。
4. リポジトリを取得する。置き場所はパスに**日本語や空白を含まない場所**を推奨する。

   ```bash
   git clone https://github.com/Sand639/Mirai01.git
   ```

5. Unity Hub の「プロジェクトを開く」で、`Mirai01/Mirai01`（`Assets` が入っている方）フォルダを選ぶ。
   - リポジトリのルートを選ぶと開けないので注意。
6. 初回起動はライブラリ生成のため時間がかかる。エラーが出た場合はリーダーかAIに相談すること。
7. `Assets/Scenes/SampleScene.unity` が開ければ成功。

> `Library/` や `Temp/` などUnityが自動生成するフォルダはGitに含めない設定になっている（`.gitignore` 済み）。
> 「Gitに出てこないファイルがある」と感じても、それは正常な場合が多い。

---

## 説明（AIが最初に行うこと）

このプロジェクトでは **Codex** や **Claude Code** などのコーディング支援AIをフル活用した制作を行う。
このREADMEを初めて読んだAIは、メンバーに対して以下の説明を丁寧に行うこと。

- Codex / Claude Codeのような「フォルダを参照して自分でファイルを編集・作成するAI」も、ChatGPTやGeminiのようなチャットAIも、**頼み方の基本は変わらない**ということ。
  ただし前者は実際にファイルが書き換わるため、**作業後は必ずGitで差分を確認してから次に進むこと**。この違いは必ず伝えること。
- Codex / Claude Codeの導入方法と、日々の使い方
- Git Flowの考え方と、このプロジェクトでの運用（後述の「Gitについて」を参照）
- 分からないことがあった時、誰に聞けばいいか

この他にも質問が来れば丁寧に教えること。専門用語はできるだけ使わず、必要なら例えを用いて説明すること。

---

## AIへの頼み方（メンバー向け）

実装を頼む時は、次の3点を伝えると精度が上がる。

1. **何を作りたいか**（機能の目的）
2. **どのファイル、どのシーンに関係するか**
3. **何ができれば完成と言えるか**

### 依頼文のテンプレート

```
【やりたいこと】プレイヤーが2段ジャンプできるようにしたい
【関係する場所】Assets/Scripts/Player 付近 / SampleScene
【完成の条件】空中で1回だけ追加でジャンプでき、着地したら回数がリセットされる
```

Gitに慣れていない場合、ブランチの作成やマージはAIに頼んでよい。コマンドを覚えていなくても作業できるようにしている。

---

## AIの注意事項

### メンバー情報の追記

- メンバーが自分の名前を伝えてきた場合、このREADMEのメンバー構成に追記すること。
- その際、役職（プログラマー／デザイナー／プランナー）が分からなければ確認すること。
- 追記したら**変更記録にも一行追加**すること。

### ドキュメントの更新

- `Documents/` 配下のドキュメントは**自動で変更してよい**。
- 変更点や改善点がある場合は、そのドキュメントの**一番下に変更ログを残したうえで**更新すること。
- ただし `Mirai01/Assets/` 以下のファイルは、指示された内容の範囲でのみ編集すること。

### Gitについて

このプロジェクトは **Git Flow** に沿って運用する。メンバー全員がGitに習熟しているとは限らないため、AIはブランチ操作のサポートを行うこと。

**ブランチの役割**

| ブランチ | 役割 |
| --- | --- |
| `main` | 展示・配布に出す完成版。直接編集しない |
| `develop` | 開発の合流先。日々の作業はここに集まる（**未作成：最初に作る**） |
| `feature/機能名` | 機能ごとの作業ブランチ。例：`feature/player-move` |
| `release/x.x` | マスターアップ前の調整用 |
| `hotfix/内容` | 展示中など、緊急の修正用 |

**運用ルール**

- 作業を始める前に、現在のブランチが作業内容に合っているかを確認する。
  合っていなければ適切なブランチ（`feature/機能名` など）の作成と切り替えを提案し、**了承を得たうえで**実行すること。
- 変更を行うたびに**自動でコミットし、プッシュまで行う**こと。コミットメッセージには、何をしたかが分かる内容を書くこと。
- `develop` へのマージはAIが行ってよい。
  **`main` へのマージは展示に出す版に直結するため、実行前にリーダーの確認を取ること。**
- コンフリクトが起きた場合は**勝手に解決せず**、どのファイルのどこが衝突したかを分かりやすく説明し、対応方針を確認すること。
- メンバーがGitの操作方法を尋ねてきた場合は、Git Flowの流れ（`feature` → `develop` → `release` → `main`）とあわせて丁寧に説明すること。
- 指示があれば、コミットを行わない、別のブランチで作業するなど、**その指示を優先する**こと。

**Unity特有の注意（重要）**

- シーン（`.unity`）とプレハブ（`.prefab`）は自動マージができないため、これらを編集した場合は**その旨を必ず報告する**こと。
- 同じシーン・同じプレハブを**2人以上が同時に編集しない**。編集前に連絡手段で「今からこのシーン触ります」と一声かける運用とする。
- 衝突すると片方の作業が失われるため、シーン作業は短時間で終わらせ、こまめにプッシュすること。

### 実装時の記録

- 実装を指示された時、実装について詳しく聞かれない場合は、**誰でもわかるように機能の説明だけ**を行うこと。
- 追加した機能の詳細は `Documents/Features/` に**1機能1ファイル**として追加すること。
- あわせて `Documents/DevLog.md` に、**日付・担当者・追加した機能・誰にでもわかる簡易的な説明**を追記すること。

### コードの方針

- プログラムの安全性やきれいさよりも、**実装できるかどうかを優先**し、複雑なコードにしないこと。
- ただし、**他の人が編集中のファイルには手を加えず**、新しい機能はできるだけ新しいファイルに書くこと。
- 指示があれば安全性や拡張性、きれいさも意識すること。
- 動作を優先した結果、安全性に懸念が残る実装になった場合は `Documents/RiskList.md` に**リスク度とその内容**を記載すること。
- 入力処理は **Input System**（`Assets/InputSystem_Actions.inputactions`）を使う。旧来の `Input.GetKey` 系は混在させない。

### バグ対応

1. 複数のファイルにまたがるバグが発生し、原因がすぐに分からなかった場合は、まず `Documents/RiskList.md` を確認し、そこに原因がないか探る。
2. 該当しそうな記載がなければ、AI自身でデバッグを行い原因を探す。
3. ユーザーの操作が必要な場合は、**丁寧な手順を提示する**こと。

### ファイル操作の禁止事項

> ⚠️ `Mirai01/Assets/` 以下のファイルを、**OS側でのファイル操作によって移動・リネーム・削除しないこと。**

Unityは `.meta` ファイルで各アセットの参照を管理しているため、これらの操作を行うと**プレハブやシーンの参照が外れる**。
必要な場合は実行せず、Unityエディタ上で行う手順を提示すること。

### セキュリティ

- APIキーやパスワード、個人情報をコードやドキュメントに書かないこと。

---

## 素材とライセンスと生成AIの扱い

使用した外部素材（画像・音・フォント・アセット）は `Documents/Credits.md` に、**名称・入手元・ライセンス**を記載すること。
**入手した時点で記載する**こと。後からまとめて調べ直すのは非常に手間がかかる。

### 生成AIの使用基準

| 対象 | 基準 |
| --- | --- |
| **デザイン・グラフィック** | 生成AIの出力をそのまま使用せず、**加工を施したうえで**使用すること |
| **プログラム** | 担当者がそのコードの**意図と動作を説明できる状態**であれば使用してよい |

理解できないままコードを組み込まないこと。バグが出た時に対応できなくなる。

判断に迷う場合はリーダーに相談すること。
※学校側に生成AIに関する規定がある場合はそれに従う（**要確認**）

---

## 未確定事項（決まり次第このREADMEを更新すること）

- [ ] 作品タイトル・ジャンル・コンセプト・プレイ人数・操作デバイス
- [ ] 対応プラットフォーム
- [ ] 機能追加を止める日
- [ ] 使用エディタ（Visual Studio / Rider）の統一
- [ ] 連絡手段（Discord など）
- [ ] メンバー構成（プログラマー4名・デザイナー3名・プランナー2名が未記入）
- [ ] `develop` ブランチの作成
- [ ] 展示用／本編ビルドの切り替え方式の決定
- [ ] 学校の生成AI規定の確認

---

## 変更記録

### 2026年

| 日付 | 変更者 | 内容 |
| --- | --- | --- |
| 8/28（金） | 大槻 海斗 | README.mdの内容の記載 |
| 8/30（日） | 大槻 海斗 | 開発環境・リポジトリ構成・初回セットアップ手順を現状に合わせて記載。ブランチ運用表、Unity特有のマージ注意、依頼テンプレート、未確定事項リストを追加。ファイル名を `README_.md` から `README.md` に変更 |
| 8/30（日） | 大槻 海斗 | `Documents/Features/_TEMPLATE.md` `DevLog.md` `RiskList.md` `Credits.md` を作成し、README記載のファイル構成を実際に用意 |
