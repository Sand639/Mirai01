# 機能と検証シーンの一覧

**いまプロジェクトにある機能を、1枚で見渡すための早見表。**

機能ドキュメントが増えて、「何があるのか」「どのシーンを開けば試せるのか」が
ファイルを1つずつ開かないと分からなくなってきたため作った（2026/9/14）。

- 詳しい中身は、各行の「ドキュメント」列のファイル（`機能ドキュメント/` の中）を読むこと。
- **機能を追加したら、ここにも1行足す。** 状態が変わったら書き換える。
- 「状態」は、**人が実際に遊んで確かめたかどうか**を基準にしている（AIはUnityの再生で操作を確かめられないため）。

---

## 機能の一覧

| 機能 | 試せるシーン（`Assets/Scenes/Test/`） | 状態 | ドキュメント |
| --- | --- | --- | --- |
| 釣りの吹き飛ばす爆弾（スタンなし・物資も飛ばす） | Scene は変更せず。`Prefabs/Sou/KnockBackBoom.prefab` を担当者が配置して確認 | 実装済み・**動作確認済み**（2026/9/22・大槻さん） | `釣りの吹き飛ばす爆弾.md` |
| 動かせるキャラクター（TPS/FPS切り替え） | `PlayerRigTest.unity` | 完成 | `プレイヤーの操作とTPS_FPS切り替え.md` |
| 物の複製と配置 | `ObjectClonerTest.unity` | 実装済み・**未確認** | `オブジェクトの複製配置.md` |
| ロボットの上半身と下半身の分離（ジャンプ・持つ・一人称） | `RobotSplitTest.unity` | 大槻さんが確認・値を調整済み | `ロボットの上半身と下半身の分離.md` |
| ロープの上り下り（縦・横） | `RobotSplitTest.unity` ／ `RobotStage/RobotStage01.unity` | つかまる条件は大槻さんが調整済み。上り下りの速さは**未確認** | `ロープの上り下り.md` |
| 踏むボタンと動く物（`MoveObject`） | `ButtonTest.unity` ／ `RobotStage/RobotStage01.unity` | 実装済み・**未確認** | `踏むボタンと動く壁.md` |
| 鍵と鍵付きの扉 | `KeyTest.unity` ／ `RobotStage/RobotStage01.unity` | 狙いの角度は大槻さんが調整済み。開く速さなどは**未確認** | `鍵と鍵付きの扉.md` |
| カメラが壁を抜けないようにする | ロボット・プレイヤーのシーンすべて | 実装済み・**未確認** | `カメラが壁を抜けないようにする.md` |
| ガラスのシェーダー | `RobotStage/RobotStage01.unity` | 実装済み・**未確認** | `ガラスのシェーダー.md` |
| ポーズ画面 | **どのシーンでも自動で出る**（Escape） | 実装済み・**修正後は未確認** | `ポーズ画面.md` |
| LANでの複数人プレイ | `LanPlayTest.unity`（**`.exe` を作って2つ起動する**） | 1台のPCで4人まで確認済み。**別PC間は未確認** | `LANでの複数人プレイ.md` |
| インターネットでの複数人プレイ（合言葉） | `LanPlayTest.unity`（同上） | 1台のPCから2人で接続を確認済み | `インターネットでの複数人プレイ.md` |
| レースのスタートとゴール（仮名：**カーレース**） | `RaceTest.unity` | 実装済み・**未確認** | `レースのスタートとゴール.md` |
| ロボットが走るレース（仮名：**月面運動会**） | `RunnerRaceTest.unity` | 走れることは大槻さんが確認済み | `ロボットが走るレース.md` |
| 重力が時間で変わる（月面運動会のメインの仕様） | `RunnerRaceTest.unity` | 実装済み・**未確認** | `重力が時間で変わる.md` |
| 重い重力で割れるガラスの床 | `GravityGlassTest.unity` | 大槻さんが値を調整済み | `重い重力で割れるガラスの床.md` |
| 爆弾 | `BombTest.unity` | **完成**（大槻さんが確認・値を調整済み） | `爆弾.md` |
| 宇宙ごみ集めの対戦（ラウンド制のチーム戦） | `SpaceJunkLobby.unity`（**ロビーから始める**。マップは `Scenes/Prototype/SpaceJunk/`） | プロトタイプ。**通しの動きとマルチプレイは大槻さんが確認済み。Map03・Map04 は作りかけなので使わない** | `宇宙ごみ集めの対戦.md` |
| 釣りフックのオートエイム（近く／遠く／手動の切り替え） | 釣りのシーンすべて（`FishingHookTest` `FishingArenaTest` `FishingOnline` `FishingMap〜`）。**作り直し不要** | 実装済み・**未確認** | `釣りフックのオートエイム.md` |
| 釣りの物資と爆発物のスポナー（**物資5：爆発物1**） | `FishingArenaTest.unity` ／ `FishingOnline.unity` ／ `FishingMap〜` | 実装済み・**未確認** | `釣りの物資のスポナー.md` |
| 釣りのマップ作りとマップ選び | `FishingLobby.unity` から選ぶ | 実装済み・**未確認** | `釣りのマップ作りとマップ選び.md` |
| 釣りの対戦用ステージ（大きな遮り） | `Assets/Scenes/Prototype/Fish/FishingMapBattle01.unity` | 実装済み・**遊んでの確認はまだ** | `釣りの対戦用ステージ.md` |

### ステージ（複数の機能を組み合わせたシーン）

| シーン | 作った人 | 中身 |
| --- | --- | --- |
| `Assets/Scenes/Prototype/RobotStage/RobotStage01.unity` | 大槻 海斗 | ロープ・踏むボタン・動く壁・鍵と扉・ガラスを組み合わせたロボットのステージ（2026/9/7）。**ビルドして実際に遊べるか検証するため、ビルドの一覧に入れてある** |
| `Assets/Scenes/Prototype/GravityRaceStage/GravityRaceStage01.unity` | 大槻 海斗 | **作成中**（2026/9/14にコミット）。ロボットとスタート位置が置いてある、月面運動会用のステージと見られる。中身の説明はまだ無い |
| `Assets/Scenes/Prototype/Fish/FishingMapBattle01.unity` | 小野田 喬 | **釣りのオンライン対戦用ステージ**（2026/9/16）。四隅の斜めの大きな壁で対角線をふさぎ、ゴールの前を長い板が横切る。**メニューで作ったシーン。遊んでの確認はまだ**（`釣りの対戦用ステージ.md`） |

> **プロトタイプのステージの置き場所は `Assets/Scenes/Prototype/` に決まった**（2026/9/14）。上のステージはどれもまだ `Test/` にあるので、移すときは Unity エディタ上でドラッグして移すこと。
>
> **ステージのシーンは作り直すツールが無い**（人が手で作ったもの）。
> **触る前に、作った人に一声かけること。**

---

## Unityのメニュー（`Tools > Mirai01`）

**いま作っている2つの遊び（釣り・宇宙ごみ集め）は直下に、それ以外は `アーカイブ` の中に
ゲームごとに分けてある**（2026/9/20。メニューが30個に増えて探しにくくなったため）。

> ⚠️ **「作り直す」と付いているものは、手で調整した内容が消える。** 押す前に確かめること。

### 釣り（直下）

| メニュー | 何をするか |
| --- | --- |
| `釣りフックの検証シーンを作る` | フックの操作だけを試す基本のシーン |
| `釣りフックのステージ検証シーンを作る` | 壁とゴールのあるステージのシーン |
| `釣りの障害物の検証シーンを作る` | 動く障害物を試すシーン |
| `釣りのオンライン用シーンを作る（ロビー＋会場）` | ロビーと会場。**オンラインはここから** |
| `釣りのスポナーを作る（ステージ検証＋オンライン会場）` | 物資と爆発物を降らせる係を組み込む |
| `釣りの新しいマップを作る（オンライン）` | 床・壁・ゴールだけの**まっさらなマップ** |
| `釣りの対戦用マップを作る（大きな遮り）` | **対戦用に形を作り込んだマップ** |
| `釣りのマップをビルドの一覧に登録し直す` | マップの名前を変えた・複製したあとに押す |
| `開いている釣りマップを点検する` | 今開いているマップに足りない物が無いか調べる |
| `釣りのオンライン検証用のビルドを作る` | `Mirai01/Build/FishingOnlineTest/` に4人で試す `.exe` |

### 宇宙ごみ集め（直下）

| メニュー | 何をするか |
| --- | --- |
| `宇宙ごみ集めのシーンを作る（ロビー＋マップ）` | ロビー・マップ4つ・プレハブ一式。**一度作ったマップは作り直さない** |
| `宇宙ごみのマップを点検する（作り直さない）` | ゴール4つ・進行役・スポナーがそろっているか調べる |
| `宇宙ごみ集めの検証用のビルドを作る` | `Mirai01/Build/SpaceJunkTest/` に複数人で試す `.exe` と `.bat`（Gitには入らない） |
| `宇宙ごみの全体固定カメラのマップを作る（Map01を複製）` | `SpaceJunkMap01` を複製し、カメラだけ**ステージ全体が映る固定カメラ**にした `SpaceJunkMap01Fixed` を作る。**一度作ったら作り直さない** |
| `宇宙ごみの自陣向きカメラのマップを作る（Map01を複製）` | `SpaceJunkMap01` を複製し、カメラだけ**自陣が画面の手前に来るよう、チームごとに向きが回る追いかけるカメラ**にした `SpaceJunkMap01TeamCam` を作る（ワイルドリフト風）。**一度作ったら作り直さない** |
| `宇宙ごみのマップの一覧を開く` | **マップの一覧**（`SpaceJunkMapList.asset`）を開く。無ければ作る。シーンをドラッグして足せば、**名前に関係なく**ロビーの候補と検証用ビルドに入る。「Show In Lobby」で候補に出す・出さないを切り替えられる |

### アーカイブ（いま手を入れていない遊び）

#### `アーカイブ > ロボットステージ`

| メニュー | 何をするか |
| --- | --- |
| `ロープを1本置く` | 縦のロープ（6m）を置く |
| `横向きのロープを置く` | 横のロープ（うんてい）を置く |
| `踏むボタンと動く壁を置く` | ボタンと壁が繋がった1組を置く |
| `鍵と鍵付きの扉を置く` | 鍵と扉の1組を置く |
| `ガラスの板を置く` | 3m × 2.5m × 8cm のガラス板を置く |
| `ロボットにロープ機能を足す` | `RobotRig.prefab` にロープ機能を足す（すでに付いていれば何もしない） |
| `ロボット分離の検証シーンを作り直す` | ⚠️ **実行しないこと。** 大槻さんが調整した `RobotRig.prefab` の値が全部初期値に戻る |

#### `アーカイブ > 月面運動会`

| メニュー | 何をするか |
| --- | --- |
| `重力で割れるガラスの床を置く` | 割れるガラスの床を1枚置く |
| `走るレースの検証シーンを作り直す` | `RunnerRaceTest.unity` を作り直す |
| `割れるガラス床の検証シーンを作り直す` | `GravityGlassTest.unity` を作り直す（プレハブは作り直さない） |

#### `アーカイブ > カーレース`

| メニュー | 何をするか |
| --- | --- |
| `レースの検証シーンを作り直す` | `RaceKart.prefab` ／ `RaceTest.unity` を作り直す |

#### `アーカイブ > 爆弾`

| メニュー | 何をするか |
| --- | --- |
| `爆弾を置く` | 爆弾を1個置く |
| `爆弾の検証シーンを作り直す` | `BombTest.unity` を作り直す。⚠️ 大槻さんが調整した重力の候補（2 / 5 / 10 / 15）が、ツールの値（1.6 / 9.81 / 14）に戻る |

#### `アーカイブ > 通信の土台`

| メニュー | 何をするか |
| --- | --- |
| `LAN通信の検証シーンを作り直す` | `NetworkPlayer.prefab` ／ `LanPlayTest.unity` を作り直す |
| `LAN検証用のビルドを作る` | `Mirai01/Build/LanTest/` に通信を試す `.exe` と `.bat`（Gitには入らない） |

#### `アーカイブ > 共通の部品`

| メニュー | 何をするか |
| --- | --- |
| `ポーズ画面を置く` | ポーズ画面を置く（**置かなくても自動で出る**。見た目をシーンごとに変えたいときだけ使う） |
| `プレイヤーの検証シーンを作り直す` | `PlayerRig.prefab` ／ `PlayerRigTest.unity` を作り直す |
| `複製配置の検証シーンを作り直す` | `ObjectClonerTest.unity` ／ 複製元プレハブ3種を作り直す |

---

## 変更ログ

| 日付 | 変更者 | 内容 |
| --- | --- | --- |
| 2026/9/14 | Claude Code | 新規作成。ドキュメント全体の点検（第2回）で、機能ドキュメントが14件に増えて全体が見渡しにくくなっていたため |
| 2026/9/14 | Claude Code | `RobotStage01.unity` がビルドに入っているのは意図したもの（ビルドして遊べるかの検証）と追記 |
| 2026/9/14 | Claude Code | プロトタイプのステージの置き場所（`Assets/Scenes/Prototype/`）が決まったことを追記 |
| 2026/9/15 | Claude Code | 釣りフックのオートエイムの行を追加（釣りのほかの機能はまだ載せていない） |
| 2026/9/16 | Claude Code | 釣りのスポナー・マップ作り・対戦用ステージの行と、釣りのマップを作るツールの表を追加 |
| 2026/9/17 | Claude Code | `develop` への取り込みにあわせて、`Scenes/Test` の重複行を消し、対戦用ステージのシーンを `Scenes/Prototype/Fish` に移したことを反映 |
| 2026/9/17 | Codex | 釣りの吹き飛ばす爆弾を追加。Scene は変更せず、配置と実際の確認は担当者が行う |
| 2026/9/17 | Codex | 吹き飛ばす爆弾を、物資も飛ばす版へ更新 |
| 2026/9/17 | Codex | Git の取り消し前の保存点から吹き飛ばす爆弾の記録を復元。担当者が保存した `Assets/Scenes/Test/Sou/FishingKnockBack.unity` も元の状態へ戻した |
| 2026/9/20 | Claude Code | 宇宙ごみ集めの対戦を一覧に追加し、シーンを作るツール（`宇宙ごみ集めのシーンを作る`）を載せた |
| 2026/9/20 | Claude Code | **`Tools > Mirai01` のメニューを整理した**（大槻さんの依頼。30個に増えて探しにくくなったため）。いま作っている**釣りと宇宙ごみ集めは直下**に残し、**それ以外の18個は `アーカイブ` の下へゲームごとに分けた**（ロボットステージ7／月面運動会3／カーレース1／爆弾2／通信の土台2／共通の部品3）。この節も新しい並びに作り替えた。**ツールの中身は変わっていない。メニューの場所だけが変わった** |
| 2026/9/20 | Claude Code | 宇宙ごみ集めのビルドを作るメニューを追加した |
| 2026/9/22 | Claude Code | 釣りの吹き飛ばす爆弾の状態を「動作確認済み」に更新 |
| 2026/9/22 | Claude Code | 宇宙ごみの全体固定カメラのマップを作るメニューを追加 |
| 2026/9/22 | Claude Code | 宇宙ごみのマップの一覧を開くメニューを追加 |
| 2026/9/22 | Claude Code | 宇宙ごみの自陣向きカメラのマップを作るメニューを追加 |
