# LANでの複数人プレイ

| 項目 | 内容 |
| --- | --- |
| 担当者 | Claude Code（依頼：大槻 海斗） |
| 作成日 | 2026年9月3日（木） |
| 最終更新日 | 2026年9月3日（木） |
| 状態 | 完成（土台として） |

---

## この機能は何か

**同じネットワークにつながっている複数のPCで、同じ空間に集まって遊べるようにする土台。**

1台を「ホスト（親）」にして、残りのPCがそこへ参加する形。
参加した人はそれぞれ自分のキャラクターを動かし、**その動きが他のPCの画面にも映る。**

**インターネットは使わない。** ハブとLANケーブルでつながっていれば動く。
展示会場のネットワークが不調でも遊べるようにするための作りである。

> **これはまだ「配線」だけで、ゲームの中身は入っていない。**
> ジャンルが決まっていなくても必要になる部分を、先に作ってある。

---

## 遊ぶ人から見た動き

| 操作 | 結果 |
| --- | --- |
| **W / A / S / D**（またはコントローラーの左スティック） | 自分のキャラクターが動く |
| **黄色い箱に触る** | **1点入る。**しばらくすると復活する |
| **茶色い箱にぶつかる** | **押せる。**全員の画面で同じように動く |
| 「LANで遊ぶ」 | ホストになるか、IPを入れて参加するかを選ぶ |
| 「1人で練習する」 | 通信を使わず1人で動かす |
| 「切断する」 | つながりを切って、最初の画面に戻る |

- 参加した順に**色が変わる**（青 → 赤 → 緑 → 黄）
- 出てくる場所も参加順にずれるので、**重なって出てこない**
- 画面左上に、**つながっている人数**と**自分の番号**が出る
- **画面右上に「通信の様子」が出る**（下を読むこと）

### 画面右上の「通信の様子」

**「4人で対戦して成立するか」を判断するための表示。**

| 出るもの | 意味 |
| --- | --- |
| **遅れ（ping）** | **相手とのやりとりに、往復で何ミリ秒かかっているか** |
| 得点 | 全員の点数。**ホストが決めた値**が全員に配られている |

**遅れの見方：**

| 数字 | どう考えるか |
| --- | --- |
| **〜30ms** | ほぼ気にならない。**LANはだいたいここ** |
| 30〜80ms | 対戦でも成立する。**判定がシビアな遊びだと気になり始める** |
| **80ms〜** | 遅れが分かる。当たり判定の作り方を考える必要がある |

**この数字を見ずに「遊べるかどうか」を話しても、感覚の議論にしかならない。**
ジャンルを決めるときの判断材料にすること。

### 何を確かめられるか

| 確かめたいこと | 見るところ |
| --- | --- |
| 位置が合っているか | 相手のカプセルが、なめらかに同じ場所を動くか |
| **得点が合っているか** | **黄色い箱を取ると、全員の画面で同じ人の点が増えるか** |
| **二重取りにならないか** | **2人で同時に1つの箱へ突っ込む。**片方にしか入らないのが正解 |
| **物理が合っているか** | **茶色い箱を押す。**全員の画面で同じ位置に転がるか |
| 遅れがどれくらいか | 右上の ping |

---

## すぐ試す方法

### A. 1台のPCで2人分ためす（一番手軽）

**エディタの再生ボタンだけでは1人分しか動かない。**
通信は「2つ以上のゲームが同時に動いている」必要があるため、`.exe` を作って2つ起動する。

1. Unityのメニュー **`Tools > Mirai01 > LAN検証用のビルドを作る`** を実行する
   （数分かかる。終わるとフォルダが自動で開く）
2. 開いたフォルダの **`2人で自動接続.bat`** をダブルクリックする

これだけで**窓が2つ立ち上がり、そのままつながった状態**になる。
両方の画面にカプセルが2つ出れば成功。片方でWASDを押すと、もう片方の画面でも動く。

> 出力先は `Mirai01/Build/LanTest/`。**Gitには入らない**ので、コミットに混ざる心配はない。

#### ボタンを押す手順も試したいとき

同じフォルダの **`2つ起動するだけ.bat`** を使う。窓が2つ出るので、

1. 1つ目で **「ホストで開始」** を押す
2. 2つ目で、IPが `127.0.0.1` になっているのを確かめて **「参加する」** を押す

> `127.0.0.1` は「このPC自身」を指す決まった番号。同じPCの中だけで試すときに使う。

> **`.bat` は何をしているか**
> `.exe` を2つ起動しているだけ。あわせて**全画面ではなく小さめの窓で開く**よう指定している
> （そのまま起動すると全画面になり、2つ並べて見られないため）。

### B. 別のPCとつなぐ（本番の形）

#### 用意するもの

| 物 | 数 | 補足 |
| --- | --- | --- |
| **スイッチングハブ** | 1台 | **5ポート以上**のものを選ぶ。数千円で買える。「ハブ」「スイッチングハブ」で売っている |
| **LANケーブル** | PCの台数分 | 1〜3mあれば足りる。**カテゴリ5e以上**なら何でもよい |
| 電源タップ | 1つ | ハブの電源が要る |

> **インターネットにつながっている必要は無い。** ハブに挿さっているPC同士が話せればよい。
> **Wi-Fiでもつながることはある**が、学校や会場のWi-Fiは
> **PC同士の通信を禁止している場合がある**ので、**有線を推奨する。**

#### 手順1：物理的につなぐ

```
   PC1（親）─┐
   PC2 ─────┤
   PC3 ─────┼── スイッチングハブ ── 電源
   PC4 ─────┘
```

全部のPCのLAN差込口とハブを、LANケーブルでつなぐだけ。
**ハブのどのポートに挿しても構わない**（ルーターと違い、区別が無い）。

#### 手順2：IPアドレスを手で決める（重要）

**ハブだけの環境には、IPアドレスを配る役がいない。**
放っておくとWindowsが仮のアドレスを勝手に付けるが、**毎回変わる**ので当日困る。
そこで**手で固定してしまう。**

Windows 11での設定：

1. **設定 > ネットワークとインターネット > イーサネット**
2. **「IP 割り当て」**の横の **「編集」**
3. **「手動」**に切り替え、**「IPv4」をオン**
4. 次のように入れる（**PCごとに最後の数字だけ変える**）

| PC | IPアドレス | サブネットマスク | ゲートウェイ・DNS |
| --- | --- | --- | --- |
| **PC1（親）** | `192.168.10.1` | `255.255.255.0` | **空欄でよい** |
| PC2 | `192.168.10.2` | `255.255.255.0` | 空欄でよい |
| PC3 | `192.168.10.3` | `255.255.255.0` | 空欄でよい |
| PC4 | `192.168.10.4` | `255.255.255.0` | 空欄でよい |

5. **保存**

> **この設定をするとインターネットにつながらなくなる。**
> 展示のときはそれで構わないが、**普段使いのPCなら、終わったら「自動 (DHCP)」に戻すこと。**

#### 手順3：つながっているか確かめる（ゲームを起動する前に）

**先にここで確かめておくと、あとの切り分けが楽になる。**

PC2でコマンドプロンプトを開き、次を打つ。

```bash
ping 192.168.10.1
```

「応答がありました」と出れば、**ネットワークは正常**。
出なければ、**ケーブル・ハブの電源・IPの設定**のどれかが間違っている。ゲームを起動しても無駄なので先に直す。

#### 手順4：ゲームをつなぐ

1. **PC1（親）**で `LanPlayTest.exe` を起動し、**「ホストで開始」** を押す
2. **PC2〜4**で `LanPlayTest.exe` を起動し、**`192.168.10.1`** を入力して **「参加する」**

**最初の起動時に、Windowsのファイアウォールの確認画面が出る。**
**「プライベートネットワーク」にチェックを入れて「アクセスを許可する」** を押すこと。
ここで「キャンセル」を押すと、以後つながらなくなる。

> **1台ずつ増やす。** いきなり4台つなぐと、どこが悪いか分からなくなる。
> まず2台でつながることを確かめてから、3台目・4台目を足す。

#### つながらないときは、ここを見る

| 症状 | 原因として多いもの |
| --- | --- |
| `ping` が通らない | **ネットワークの問題。**ケーブルの挿し忘れ、ハブの電源、IPの入れ間違い。ゲーム以前の話 |
| `ping` は通るのに参加できない | **ファイアウォール。**初回の確認画面で許可しなかった可能性が高い。`Windows セキュリティ > ファイアウォールとネットワーク保護 > アプリケーションの許可` から `LanPlayTest` を探して、**プライベート**にチェックを入れる |
| そもそもIPが違う | `127.0.0.1` のままだと**自分自身につなぎに行く**。親のIPに書き換える |
| IPが複数出ている | 有線とWi-Fiの両方がつながっている。**検証中はWi-Fiを切る**と分かりやすい |
| つながるがすぐ切れる | ポート番号が違っていないか確認する（全PCとも `7777`） |

**ログにも記録が残る。** `[LAN]` で始まる行を見れば、どこまで進んで失敗したかが分かる。

---

## 展示当日の使い方（起動しただけでつながる）

毎回ボタンを押して回るのは手間なので、**ショートカットに引数を書いておく**と、
起動しただけで自動的につながる。

ショートカットを右クリック →「プロパティ」→「リンク先」の末尾に足す。

| PC | リンク先の末尾に足すもの |
| --- | --- |
| 親のPC | `-host` |
| 参加するPC（3台） | `-client 192.168.10.1` ← 親のIPに置き換える |

ポートを変えたいときは、続けて `-port 7777` のように書く。

> **親のIPは、上の「手順2」で固定しておくこと。** 毎回変わると、当日ショートカットを書き直すはめになる。

---

## 関係するファイル・シーン

| 種類 | パス |
| --- | --- |
| スクリプト | `Assets/Scripts/Network/NetworkPlayer.cs`（プレイヤー1人分） |
| スクリプト | `Assets/Scripts/Network/LocalPlayerCamera.cs`（自分を追いかけるカメラ） |
| スクリプト | `Assets/Scripts/Network/LanConnectionUi.cs`（接続の仮画面） |
| スクリプト | `Assets/Scripts/Network/LanAutoStart.cs`（起動引数での自動接続） |
| スクリプト | `Assets/Scripts/Network/LanConnectionLogger.cs`（接続状況のログ） |
| スクリプト | `Assets/Scripts/Network/NetworkStatusHud.cs`（遅れと得点の表示） |
| スクリプト | `Assets/Scripts/Network/PlayerScore.cs`（得点。ホストのみ書き換え可） |
| スクリプト | `Assets/Scripts/Network/PickupItem.cs`（触ると点が入るアイテム） |
| スクリプト | `Assets/Scripts/Network/PushableBox.cs`（みんなで押せる箱） |
| スクリプト（エディタ用） | `Assets/Scripts/Network/Editor/LanPlayTestSetup.cs`（シーン生成ツール） |
| スクリプト（エディタ用） | `Assets/Scripts/Network/Editor/LanTestBuilder.cs`（検証用ビルド作成ツール） |
| プレハブ | `Assets/Prefabs/NetworkPlayer.prefab` |
| シーン | `Assets/Scenes/Test/LanPlayTest.unity` |
| マテリアル | `Assets/Art/Materials/NetworkPlayerBody.mat` ほか2つ |
| 入力設定 | `Assets/InputSystem_Actions.inputactions`（**既存のものを使用。変更していない**） |
| 通信ライブラリ | `com.unity.netcode.gameobjects` 2.13.2（`Packages/manifest.json`） |

> シーン（`.unity`）とプレハブ（`.prefab`）は**同時に2人以上で編集できない**。
> 触るときは担当者に一声かけること。

### シーンの中身

```
LanPlayTest
├── Directional Light
├── Ground                  ← 床
├── Marker_North / South / East / West   ← 動いたことが分かる目印
├── Step                    ← 段差（登れるかの確認用）
├── Item_North / South / East / West     ← 黄色。触ると1点入る
├── PushBox_1 / PushBox_2                ← 茶色。みんなで押せる
├── Main Camera             ← LocalPlayerCamera（自分を追いかける）
└── NetworkManager          ← 通信のまとめ役
    ├─ NetworkManager       … 通信の本体。ここに NetworkPlayer.prefab を登録してある
    ├─ UnityTransport       … 実際の通信部品。IPとポートを持つ
    ├─ LanConnectionUi      … 接続の仮画面
    ├─ LanAutoStart         … 起動引数での自動接続
    ├─ LanConnectionLogger  … 接続状況のログ
    └─ NetworkStatusHud     … 遅れと得点の表示
```

**プレイヤーはシーンに置かれていない。** つないだ瞬間に、人数分が自動で作られる。

**アイテムと箱は、最初からシーンに置いてある。**
ホストが始めた瞬間に通信の対象になり、あとから参加した人にも自動で配られる。

---

## 設定できる値（インスペクターの項目）

シーンの `NetworkManager` を選ぶと出てくる。

### Lan Connection Ui

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Address** | 参加するときの、つなぎ先のIP | 127.0.0.1 |
| **Port** | 使うポート番号。**全PCで同じ値にすること** | 7777 |
| Ui Scale | 画面表示の大きさ。**展示の大きい画面では上げる** | 1.5 |
| Show Ui | OFFにすると何も表示しない（本番の画面ができたら切る） | ON |

### Lan Auto Start

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Auto Host In Editor | ONにすると、エディタでの再生時に自動でホストになる | OFF |

### Network Status Hud（画面右上の表示）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Show Hud** | OFFにすると表示しない。**本番では切る** | ON |
| Ui Scale | 表示の大きさ | 1.5 |
| Refresh Seconds | 表示を更新する間隔（秒）。細かすぎると読めない | 0.25 |

### Pickup Item（黄色いアイテム。シーンで選ぶ）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Pickup Radius** | **この距離まで近づくと取れる（m）。**シーン画面に黄色い丸で出る | 1.2 |
| Check Height | 高さの差を見るか。OFFだと真上や真下からでも取れる | ON |
| Height Tolerance | 高さの差をどこまで許すか（m） | 2 |
| Score Amount | 取ったときに入る点数 | 1 |
| **Respawn Seconds** | **取られてから復活するまでの秒数** | 5 |
| Spin Speed | 見た目を回す速さ | 90 |

### Pushable Box（茶色い箱。シーンで選ぶ）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Push Acceleration** | **押されている間の加速の強さ。大きいほどキビキビ動く** | 25 |
| **Max Speed** | **これ以上の速さでは動かない（m/秒）** | 5 |
| Horizontal Only | ONで水平方向にだけ押す（上に飛ばない） | ON |
| Push Hold Seconds | 指示が届いてから「まだ押している」とみなす秒数 | 0.3 |

### Network Player（押す側の設定）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Push Send Interval | 「押している」をホストへ伝える間隔（秒） | 0.1 |

> **押し心地を変えたいときは、まず `Push Acceleration`（キビキビ感）と
> `Max Speed`（最高速度）の2つを触ること。**
> 箱の重さ（Rigidbody の Mass）を変えても押し心地は変わらない作りにしてある
> （質量に関係なく同じ加速になるため）。

### Network Player（プレハブ側）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Move Speed | 進む速さ（1秒あたりのメートル） | 5 |
| Turn Speed | 進む方向へ向き直る速さ | 720 |
| Gravity | 落ちる強さ。**マイナスの値にすること** | -20 |
| **Player Colors** | 参加順に割り当てる色。**4色入っている** | 青／赤／緑／黄 |
| Spawn Radius | 中心からどれだけ離れた場所に出てくるか（m） | 3 |

---

## 仕組み（分かる人向け）

- 通信は **Netcode for GameObjects**（Unity公式）を使っている
- **ホスト方式**。1台が「サーバー兼プレイヤー」を兼ねる。専用サーバーは要らない
- 位置合わせは **`NetworkTransform` の Owner 権限**。
  **動かしている本人が位置を送り、他のPCはそれを再現する**形にしている
- 自分以外のキャラクターは、`CharacterController` を切ってある。
  切らないと、送られてきた位置へ移そうとしても押し戻してしまうため
- **色は通信で送っていない。** 参加番号から計算しているので、どのPCで見ても同じになる
- **得点はホストだけが書き換えられる**（`NetworkVariable` の初期設定）。
  参加者側から自分の点を勝手に増やすことはできない
- **アイテムを取った判定も、ホストだけが行う。** 参加者のPCでは近づいても何も起きない。
  そのため、**2人が同時に触っても二重取りにならない**（一番近い1人にだけ入る）
- **アイテムは「当たり判定」ではなく「距離」で取る。**
  最初は当たり判定で作ったが、**ホスト以外が取れない**不具合が出た。
  自分以外のキャラクターは `CharacterController` を切ってあり、
  **切ると当たり判定ごと無くなる**のが原因だった。
  位置は全員分きちんと届いているので、距離を測るほうが確実
- **押せる箱の物理も、ホストだけが回す。** 参加者側は `isKinematic` で止めてある。
  全員がそれぞれ物理を回すと、同じ入力でも結果が少しずつずれて、位置がバラバラになるため
- 箱を押すときは、**ぶつかった人がホストに「押した」と伝える**（`ServerRpc`）。
  自分では力を加えない
- **箱は「叩く」のではなく「押し続ける」。**
  ぶつかっている人が「押している向き」を0.1秒ごとに送り、
  **ホストはそれを人ごとに預かって、押している間ずっと力を加え続ける。**
  最初は1発ずつ叩く形にしていたが、①吹っ飛ぶ ②直したら重すぎる ③人によって効きが違う、
  と3回作り直した。**指示が1回落ちても次で埋まる**ので、通信のゆらぎに強い
- 接続部品（`UnityTransport`）のIPとポートだけを差し替える作りなので、
  **将来インターネット対応（Unity Relay）を足すときも、ゲーム側は書き換えずに済む**

### なぜ Netcode for GameObjects を選んだか

| 候補 | 判断 |
| --- | --- |
| **Netcode for GameObjects** | **採用。**Unity公式・無料で、**インターネット無しのLANだけで動く** |
| Mirror | 次点。LANで動くが、公式ではない |
| PUN 2 | **不採用。**クラウド経由のため**インターネットが必要**。メーカー自身も新規はFusionを推奨している |
| Photon Fusion | 不採用。同じくクラウド前提 |

**決め手は「当日インターネットが無くても遊べること」。**
展示会場のネットワークは自分たちで制御できないため。

---

## できていないこと・既知の問題

- **別PC同士での確認をしていない。**
  同じPCの中で2つ起動してつながることは**実際に確認済み**（ログで2人分の生成を確認）。
  ただし**LANケーブルで別のPCとつなぐ確認は、私（AI）にはできない**ので未実施
- ~~4人同時は未確認~~ → **2026/9/3に、1台のPCで4つ起動して確認済み。**
  4色とも出て、番号も 0〜3 まで正しく割り当てられた（大槻さんが確認）
- **画面が仮のもの。** 接続の画面は簡易表示で作ってある。**本番のタイトル画面はあとで作り直す前提**
- **視点が回せない。** カメラは後ろから追いかけるだけで、マウスや右スティックでは回らない
- ~~アイテムを取る・箱を押す動きが未確認~~ → **2026/9/3に大槻さんが確認済み。**
  **全員がアイテムを取れること**と、**ホストと参加者で同じように箱を押せること**を確認した
  （途中で2件の不具合が見つかり、修正してある。下の「変更ログ」を参照）
- **2人が同時に1つのアイテムへ触ったときの動きは、まだ確認できていない。**
  一番近い1人にだけ入る作りだが、**1台のPCでは2人分を同時に操作できない**ため試せない。
  **メンバーが集まったときに確かめる予定**
- **ズルへの対策は、移動だけ残っている。** **得点と物理の判定はホストに閉じてある**ので、
  点数を書き換えるようなズルは効かない。ただし**位置は本人が申告する**ため、
  改造すれば壁抜けはできてしまう。
  展示のように**閉じたLANで、目の前の4人だけで遊ぶ**分には問題にならないと判断している
- **音・演出・名前表示が無い。** 誰が誰かは色だけで見分ける
- **切断したあとの再接続を試していない。** 途中で抜けた人がもう一度入れるかは未確認

---

## 作り直したいとき

**`Tools > Mirai01 > LAN通信の検証シーンを作り直す`**

プレハブ・シーン・マテリアルがまとめて作り直される。
**手で調整した内容は消える**ので、実行前に確認すること。

---

## 変更ログ

| 日付 | 変更者 | 内容 |
| --- | --- | --- |
| 2026/9/3 | Claude Code | 新規作成。LANでの接続・プレイヤーの同期・接続画面・起動引数での自動接続まで |
| 2026/9/3 | Claude Code | 1台のPCで確かめる手順を、**`.bat` をダブルクリックするだけ**に簡単にした。ビルドの出力先を `Mirai01/Build/LanTest/`（Gitに入らない場所）に変更 |
| 2026/9/3 | Claude Code | **1台のPCで4人同時につながることを確認**（大槻さんによる。4色とも表示、番号0〜3）。あわせて、**別PCとつなぐときの機材・配線・IPの固定手順**を追記 |
| 2026/9/3 | Claude Code | **中身の同期を確かめる要素を追加。** 遅れ(ping)と得点の表示、触ると点が入るアイテム4個、みんなで押せる箱2個。**判定と物理はホストだけが行う**形にしてある。あわせて「遊び方を選ぶ画面」を追加（LAN／インターネット(準備中)／1人で練習） |
| 2026/9/3 | Claude Code | **不具合2件を修正。** ①**参加者がアイテムを取れなかった**（自分以外は `CharacterController` を切っており、当たり判定ごと無くなっていた）→ **ホストが距離を測る形**に変更。②**参加者が押すと箱が吹っ飛んだ**（毎フレーム「押した」を送っていた）→ **送る側とホスト側の両方で間隔を制限**し、速度の頭打ちも追加 || 2026/9/3 | Claude Code | **不具合2件を修正。**①**参加者がアイテムを取れなかった**（自分以外は `CharacterController` を切っており、当たり判定ごと無くなっていた）→ **ホストが距離を測る形**に変更。②**参加者が押すと箱が吹っ飛んだ**（毎フレーム「押した」を送っていた）→ **送る側とホスト側の両方で間隔を制限**し、速度の頭打ちも追加 |
| 2026/9/3 | Claude Code | **箱の押し方を「叩く」から「押し続ける」に作り直した。** 前回の修正で重くなりすぎ、しかも制限を箱ごとに1つしか持たず**押す人同士で奪い合っていた**。「押している向き」を人ごとに預かり、押している間ずっと力を加える形に変更 |
