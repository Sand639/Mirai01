# ポーズ画面

| 項目 | 内容 |
| --- | --- |
| 担当者 | Claude Code（依頼：大槻 海斗） |
| 作成日 | 2026年9月7日（月） |
| 最終更新日 | 2026年9月20日（日） |
| 状態 | 実装済み（**まだ遊んで確かめていない**） |

---

## この機能は何か

**Escape を押すと開く、ポーズ画面。** 開いている間、**自分の操作は受け付けなくなる。**

**1人で遊んでいるときは、世界の時間も止まる。**
**通信でつながっているときは止まらない**（他の人は動き続けているため）。

| 項目 | 押すとどうなるか |
| --- | --- |
| **ゲームをつづける** | 画面を閉じて、続きから遊ぶ |
| **設定** | 音量とマウス感度を変える画面へ |
| **ゲームをやめる** | ゲームを終了する（**エディタでは再生を止めるだけ**） |

**マウスで選ぶ。** もう一度 Escape を押しても閉じられる。

---

## 遊ぶ人から見た動き

| 操作 | 結果 |
| --- | --- |
| **Escape** | **ポーズ画面が開く。** 後ろが暗くなり、自分の操作が止まる（1人で遊んでいるときは世界の時間も止まる）。マウスカーソルが出る |
| **項目にマウスを重ねる** | **色が変わり、少し大きくなり、左に「▶」が出る** |
| **項目をクリック** | その項目が実行される |
| **Escape**（開いている間） | 閉じて、続きから遊ぶ |

**閉じるとカーソルは消えて、視点操作に戻る。**

### マウスが重なったときの見せ方

**3つ同時に変えている。** 1つだけだと気づかれにくいため。

- **色**が明るくなる
- **少しだけ大きくなる**（5%）
- **左に「▶」が出る**

変化は**なめらかに**動く。ポーズ中でも動くように、**止まっても進む時間**を使っている。

### 設定の画面

| 項目 | 内容 |
| --- | --- |
| **音量** | ゲーム全体の音の大きさ |
| **マウス感度** | 視点を回す速さの倍率（1でそのまま） |

**次に遊ぶときも、変えた値は残る**（パソコンに保存している）。

---

## 関係するファイル・シーン

| 種類 | パス |
| --- | --- |
| スクリプト | `Assets/Scripts/UI/PauseMenu.cs`（**ポーズ画面**） |
| スクリプト | `Assets/Scripts/UI/MenuHoverHighlight.cs`（マウスが重なったときの見せ方） |
| スクリプト | `Assets/Scripts/Common/GamePause.cs`（**止まっているかどうか**） |
| スクリプト | `Assets/Scripts/Common/GameSettings.cs`（音量・マウス感度） |
| スクリプト | `Assets/Scripts/Robot/RobotCameraLook.cs`（止まっている間は視点を触らない） |
| スクリプト | `Assets/Scripts/Player/PlayerController.cs`（同上） |
| エディタ用 | `Assets/Scripts/UI/Editor/PauseMenuSetup.cs`（**置くツール**） |
| プレハブ | `Assets/Prefabs/Common/PauseMenu.prefab` |

> **UI用のスクリプトは `Assets/Scripts/UI/` にまとめてある**（2026/9/7に作成）。
> ステージの仕掛けでもロボット専用でもないため。

---

## シーンに置く手順

**置かなくてよい。** どのシーンでも、**再生すると自動で用意される。**

置き忘れたまま「Escape を押しても何も起きない」と悩むことが多いため、
**シーンのどこにも無ければ、ゲーム側が自分で1つ作る**ようにしてある。

見た目や色を**そのシーンだけ変えたい**ときは、手で置けばそちらが使われる。

1. 置きたいシーンを開く
2. メニューの **`Tools > Mirai01 > ポーズ画面を置く`** を実行

**カメラもUIも要らない**（足りない物はこちらで作る）。

> **止まっている間は、シーンに何も見えない。**
> 画面は**再生したときに組み立てられる**ため（理由は下に書いてある）。
> 再生して Escape を押すと出てくる。

---

## 設定できる値（インスペクターの項目）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Pause Key | 開く／閉じるキー | Escape |
| Input Actions | `Assets/InputSystem_Actions`。**UIのクリックに使う** | 自動で入る |
| Font Names | 使いたい日本語フォントの名前。**上から順に探す** | 游ゴシックなど6つ |
| Backdrop Color | 後ろを暗くする色 | 黒・65% |
| Item Color | 項目の色（ふだん） | 濃い灰色 |
| **Item Hover Color** | **項目の色（マウスが重なったとき）** | 青 |
| Text Color | 文字の色 | 白 |
| Font Size | 項目の文字の大きさ | 28 |

---

## 仕組み（分かる人向け）

### 画面をコードで組み立てている理由

**プレハブに画面を作り込んでいない。** 再生したときに、`PauseMenu` が自分で作る。

理由は**日本語のため。**
Unityに最初から入っている文字は**日本語を持っていない**ので、
そのままだと項目名が**四角（□□□）**になってしまう。

**パソコンに入っている日本語フォントを借りてくる**必要があるが、
それは**保存できない**（プレハブに残らない）。
そのため、画面を作るところまで含めてコードにしてある。

> **日本語のフォントを持つ形（TextMeshProの日本語フォント）を用意すれば、
> 普通にプレハブで作れるようになる。** 見た目を凝りたくなったら、そのとき作り直すのがよい。

### Escape の取り合い

**Escape は、もともとカメラ側が「カーソルを出す」ために使っていた。**
そのままだと、ポーズ画面と取り合いになる。

**「止まっている間だけカメラが手を引く」では足りなかった。**
閉じる瞬間は**同じ1フレームの中で**「止まっていない」に戻るため、
そのあとにカメラ側が同じ Escape をもう一度拾ってしまい、
**閉じたのにカーソルが出たまま**になっていた。

そこで、**ポーズ画面があるなら Escape はカメラ側では一切見ない**ことにした。

| | ポーズ画面があるとき | 無いとき |
| --- | --- | --- |
| カメラ | **Escape を見ない**（クリックでカーソルを固定するのは今までどおり） | Escape でカーソルを出す |
| ポーズ画面 | Escape で開く／閉じる | ― |

あわせて、**ポーズ画面は他の部品より先に動く**ようにした（`DefaultExecutionOrder(-100)`）。
順番が決まっていないと、同じフレームでの取り合いが再び起きるため。

**複製配置システムも Escape を「やめる」に使っている。**
止まっている間は入力を受け取らないようにしたので、
ポーズ画面を開いても**置きかけの複製が消えない。**

> ⚠️ **ただし、閉じるときは同じ取り合いが残っている可能性がある**（2026/9/14の点検でコードを読んで気づいた。**未確認**）。
> Escape で閉じると、そのフレームで「止まっていない」に戻り、あとから動く複製配置が**同じ Escape を拾って置きかけの物を消す**かもしれない。
> 「ゲームをつづける」をクリックして閉じた場合も、**同じクリックで物が置かれる**かもしれない。
> 結果として、**複製配置の「Escape でやめる」はいまは使えない**。`リスクリスト.md` に登録してある。

### 止まっているのに動かしたいもの

1人で遊んでいるときは `Time.timeScale` を 0 にするので、**歩く・落ちるは自動的に止まる。**

> **通信でつながっている間は `Time.timeScale` を変えない**（2026/9/20）。
> 自分のPCだけ時間を止めても他の人は動き続けるので、閉じた瞬間に相手が飛んで見える。
> ホストが止めると、**ホストが動かしている物が全員の画面で空中に止まる**のに、
> 試合の残り時間（共有の時計で数えている）だけは減り続けてしまう。
> そのため、**つながっている間は「自分の操作だけ止める」**形にしてある
> （`GamePause.SetPaused` が `NetworkManager.Singleton.IsListening` を見て分ける）。
> **止まらないぶん、入力を読む側が `GamePause.BlocksInput` を見ていることが大事になる。**

止まっているときは、2つだけ自分で気をつける必要がある。

- **マウスでの視点移動は止まらない**（動いた量をそのまま使っているため）
  → `GamePause.IsPaused` を見て止めている
- **`Time.deltaTime` が 0 になる**ので、そのままだとUIのアニメーションも止まる
  → マウスが重なったときの変化は `Time.unscaledDeltaTime` を使っている

### クリックを受け取る仕組み

UIのクリックには `EventSystem` が要る。**無ければ自動で作る。**

このプロジェクトは**新しい入力方式（Input System）**を使っているので、
古い受け取り方（`StandaloneInputModule`）を付けるとエラーになる。
**`InputSystemUIInputModule`** のほうを付けている。

---

## できていないこと・既知の問題

- **見た目の大きさや色は調整前の値。** 遊びながら詰めること
- **自動で用意される画面は、インスペクターの値が初期値のまま**になる。
  色や文字の大きさを変えたいシーンでは、`ポーズ画面を置く` で手で置くこと
- **フォントはパソコン任せ。** 游ゴシックなどが入っていないパソコンでは、
  別のフォントになるか、文字が四角になる可能性がある
- **キーボードやコントローラーでは選べない。** いまはマウス専用（本番の操作は基本コントローラーなので、本番用に作るときは対応が要る。プロトタイプの段階ではこのままでよい）
- **設定は音量とマウス感度の2つだけ。** 画面の大きさや操作の割り当ては未対応
- **「ゲームをやめる」に確認画面が無い。** 押すとすぐ終了する
- **音は止まらない。** 音を入れたら、`GamePause` で `AudioListener.pause` も切り替えること
- 通信（マルチプレイ）では、**世界の時間は止まらない**（2026/9/20）。
  止まるのは自分の操作だけで、他の人と物は動き続ける。
  自分だけ時間を止めると、閉じた瞬間に相手が飛んで見えるため

---

## 変更ログ

| 日付 | 変更者 | 内容 |
| --- | --- | --- |
| 2026/9/7 | Claude Code | 新規作成 |
| 2026/9/8 | Claude Code | **Escape でうまく開かない問題を直した**（大槻さんの報告）。(1) **置き忘れても動く**ように、シーンに無ければ自動で用意する (2) **カメラ側は Escape を見ない**ようにして、閉じた直後にカーソルが出たままになるのを直した (3) ポーズ画面を**他の部品より先に**動かした (4) 閉じたとき、**開く前のカーソルの状態に戻す**ようにした (5) 複製配置システムが、止まっている間は入力を受け取らないようにした |
| 2026/9/10 | Claude Code | 日本語の文字を用意する処理を `Assets/Scripts/Common/UiFont.cs` に切り出した（レースの画面表示でも同じものが必要になったため）。動きは変わらない |
| 2026/9/14 | Claude Code | **ドキュメント点検（第2回）。** ポーズ画面を**閉じる**フレームで、複製配置と Escape・クリックを取り合う可能性があることを「Escape の取り合い」に追記した（コードを読んで気づいたもので、未確認）。変更ログに 2026/9/10 の行があるのに最終更新日が 9/8 のままだったのを直した |
| 2026/9/14 | Claude Code | 操作デバイスの回答（本番は基本コントローラー）を既知の問題に反映 |
| 2026/9/16 | Claude Code | **閉じた瞬間に、同じ入力を二重に拾う不具合を直した。** GamePause に「切り替わったフレームか」を持たせ、入力を読む側が見る BlocksInput を追加。複製配置で下書きが消える／勝手に置かれることが無くなった。**オンラインのシーンにポーズ画面を足すときの注意**（自分だけ時間が止まる）を リスクリスト.md に登録 |
| 2026/9/16 | Claude Code | **Freeze Time を追加。** OFFにすると世界の時間を止めず、操作だけ受け付けない。**オンラインのシーンではOFFにすること** |
| 2026/9/20 | Claude Code | **シーンを切り替えたあと、ボタンが反応しなくなる不具合を直した**（大槻さんの報告）。ポーズ画面そのものは `DontDestroyOnLoad` で残るのに、**クリックを受け取る `EventSystem` だけがシーンと一緒に消えていた**（`EnsureEventSystem` は `Awake` で1回しか呼ばれないので、作り直されることもなかった）。ロビーでは同じシーンにいるため気づけず、**オンラインで会場へ移った瞬間に効かなくなる**形で出ていた。`EventSystem` にも `DontDestroyOnLoad` を付け、**開くたびに、無ければ作り直す**ようにした。釣り・宇宙ごみの両方で起きていた |
| 2026/9/20 | Claude Code | **オンラインでつながっている間は、世界の時間を止めないようにした。** 以前はホストが開くと `Time.timeScale` が 0 になり、**ホストが動かしている物が全員の画面で空中に止まるのに、試合の残り時間だけは減り続けていた**（`リスクリスト.md` 2026/9/17）。`GamePause.SetPaused` の中で `NetworkManager.Singleton.IsListening` を見て分けるようにしたので、**呼ぶ側が Freeze Time を指定し忘れても事故にならない。** 1人で遊ぶときは今までどおり止まる |
