# レースのスタートとゴール

| 項目 | 内容 |
| --- | --- |
| 担当者 | Claude Code（依頼：大槻 海斗） |
| 作成日 | 2026年9月10日（木） |
| 最終更新日 | 2026年9月10日（木） |
| 状態 | 実装済み（**まだ遊んで確かめていない**） |
| **仮名** | **カーレース**（大槻さんが命名。2026/9/10） |
| 関連 | **ロボットが走る版（仮名：月面運動会）**もある → `ロボットが走るレース.md` |

---

## この機能は何か

**マリオカートのような「スタートして、走って、ゴールする」の土台。**

カウントダウンのあとに走り出し、**コースを1周するとゴール**になる。
タイムと順位が画面に出る。

**あとでマルチプレイにする予定**があるため、
**1人でも複数人でも同じ形で動く**ように作ってある（下の「通信のための備え」を読むこと）。

> **中身はまだ「レースの骨組み」だけ。**
> アイテムも、加速パネルも、コンピューターの相手もいない。
> **順番の数え方と、勝ち負けの決め方**を先に作ってある。

---

## 遊ぶ人から見た動き

| 操作 | 結果 |
| --- | --- |
| **W / ↑** | アクセル |
| **S / ↓** | ブレーキ。止まっている間は**バック** |
| **A / D（← →）** | ハンドル |
| **R** | **やり直し**（スタート地点に戻る） |
| **Escape** | ポーズ画面（`ポーズ画面.md`） |

### 流れ

1. 再生すると、カートは**スタート地点に並んで止まっている**
2. **3・2・1** のカウントダウン。この間はアクセルを踏んでも進まない
3. **「スタート！」** で走り出せる
4. コースを**1周**すると **「ゴール！」** が出て、**タイムと順位**が表示される
5. **R** でいつでもやり直せる

### 画面に出るもの

| 場所 | 出るもの |
| --- | --- |
| 左上 | **周回**（1 / 1） |
| 右上 | **タイム** |
| 中央 | カウントダウン、ゴールの結果 |
| 右下 | 速度（km/h） |
| 左下 | 操作の案内 |

---

## すぐ試す方法

1. Unityで `Assets/Scenes/Test/RaceTest.unity` を開く
2. **再生ボタンを押す**

コースもカートも置いてあるので、そのまま走れる。

---

## 関係するファイル・シーン

| 種類 | パス |
| --- | --- |
| スクリプト | `Assets/Scripts/Race/RaceManager.cs`（**進行役。周回を数える**） |
| スクリプト | `Assets/Scripts/Race/RaceRacer.cs`（1台ぶんの記録） |
| スクリプト | `Assets/Scripts/Race/RaceCheckpoint.cs`（**通過点**） |
| スクリプト | `Assets/Scripts/Race/KartController.cs`（**運転**） |
| スクリプト | `Assets/Scripts/Race/RaceCamera.cs`（後ろから追うカメラ） |
| スクリプト | `Assets/Scripts/Race/RaceHud.cs`（画面表示） |
| スクリプト | `Assets/Scripts/Common/UiFont.cs`（**日本語の文字を用意する。ポーズ画面と共通**） |
| エディタ用 | `Assets/Scripts/Race/Editor/RaceCourseSetup.cs`（**コースを作り直すツール**） |
| プレハブ | `Assets/Prefabs/RaceKart.prefab` |
| シーン | `Assets/Scenes/Test/RaceTest.unity` |
| シーン | `Assets/Scenes/Test/RunnerRaceTest.unity`（**ロボットが走る版**。`ロボットが走るレース.md`） |
| マテリアル | `Assets/Art/Materials/RaceRoad.mat` ほか5つ（`RaceWall` / `RaceGround` / `RaceStartLine` / `RaceKartBody` / `RaceKartWheel`） |
| 入力設定 | `Assets/InputSystem_Actions.inputactions`（**既存のものを使用。変更していない**） |

> シーン（`.unity`）とプレハブ（`.prefab`）は**同時に2人以上で編集できない**。
> `RaceTest.unity` と `RaceKart.prefab` を触るときは、担当者に一声かけること。

### シーンの中身

```
RaceTest
├── Directional Light
├── Ground              ← 道の外側の地面
├── Course
│   ├── Road            ← 道（96枚の板をカーブに沿って並べたもの）
│   ├── Walls           ← 左右の壁
│   ├── Checkpoints     ← 通過点6つ。**0番がスタート／ゴールの線**
│   ├── StartGrid       ← スタート位置4つ（**通信で人数が増えたとき用**）
│   └── StartLineMark   ← 白い線（見た目だけ）
├── RaceManager         ← 進行役
├── RaceHud             ← 画面表示
├── Main Camera         ← RaceCamera
└── RaceKart            ← カート
```

---

## コースの形

**陸上のトラックと同じ形。** 直線2本と、半円2つ。

| 項目 | 値 |
| --- | --- |
| 直線の長さ | 70m |
| カーブの半径 | 34m |
| 道の幅 | 16m |
| **1周の長さ** | **約354m** |

**手で並べていない。** 道・壁・通過点は、すべて**同じ計算式から位置を出している。**
手で置くと道と通過点がずれて、**「通ったのに数えてくれない」**という直しにくい不具合になるため。

大きさを変えたいときは `RaceCourseSetup.cs` の上のほうにある数値を変えて、
メニューの **`Tools > Mirai01 > レースの検証シーンを作り直す`** を実行する。

> **作り直すと、手で調整した内容は消える。** 実行する前に確認すること。

---

## 設定できる値（インスペクターの項目）

### RaceManager（進行役）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Laps To Finish** | **何周でゴールか** | **1** |
| Ready Seconds | 再生してからカウントダウンが始まるまで | 1.5秒 |
| Countdown Seconds | カウントダウンの秒数 | 3 |
| Start Automatically | 再生したら勝手に始めるか | ON |
| Checkpoints | **通過点を、通る順番に入れる。0番目がスタート／ゴール** | 自動で入る |
| Start Grid | スタート位置。上から順に割り当てる | 4つ |
| Restart Key | やり直しのキー | R |

### KartController（運転）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Max Speed | 前に進む最高速度（m/秒） | 20 |
| Max Reverse Speed | バックの最高速度 | 6 |
| Acceleration | 速くなる勢い | 14 |
| Brake Power | ブレーキの効き | 28 |
| Coast Deceleration | 何も押していないときに減る勢い | 7 |
| Turn Speed | 1秒あたりに回れる角度 | 115 |
| Turn Warmup Speed | この速さまではハンドルが効きにくい | 4 |
| High Speed Turn Damp | 速いときにハンドルを鈍くする量 | 0.35 |
| Wall Speed Keep | 壁に当たったときに残る速さ | 0.3 |

### RaceCheckpoint（通過点）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Order** | **通る順番。0番がスタート／ゴールの線** | 自動で入る |
| Width | 線の幅 | 20m |
| Height | 線の高さ | 10m |

### RaceCamera（カメラ）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Target | 追う相手。**空なら自分のカートを探す** | 空 |
| Distance | 後ろに下がる距離 | 7.5m |
| Height | 上に上がる高さ | 3.4m |
| Position / Rotation Smooth | 付いていく滑らかさ | 7 / 10 |

---

## 仕組み（分かる人向け）

### 1周をどう数えているか

**通過点（チェックポイント）を、順番に全部通ったら1周。**

コースには通過点が**6つ**あり、**0番がスタート／ゴールの線**。
カートは「次に通るべき番号」を1つだけ覚えていて、
**その番号の線を通ったときだけ**次へ進む。

```
スタート → 1 → 2 → 3 → 4 → 5 → 0（ゴール線）＝ 1周
```

こうすると**近道ができない。** 途中の壁を越えてゴール線へ戻っても、
4番や5番を通っていないので数えてもらえない。

> **スタート地点はゴール線の手前**にある。
> 走り出してすぐゴール線を通るが、そのときの「次の番号」は1番なので、**1周とは数えない。**

### 当たり判定を使っていない理由

**速いと通り抜けるから。**

時速70kmだと1フレームで30cm以上進む。
薄い板に当たり判定を付けても、**その板を飛び越して次のフレームに行ってしまう**ことがある。

ここでは**「前のフレームの位置」から「今の位置」までの線が、通過点の面を横切ったか**を計算している。
**どれだけ速くても取りこぼさない。** 逆走して戻ってきた場合は数えない（向きも見ている）。

> **これは通信でも効く。** 通信では自分以外の当たり判定が消えていることがあり、
> 当たり判定に頼る作りは動かなくなる（`Documents/AIの申し送り.md` に失敗例あり）。

### 物理演算を使っていない理由

カートは **CharacterController** で動かしている。Rigidbody（物理演算）ではない。

- このプロジェクトでは、**物理と手動の動かし方を混ぜるとガタつく**ことが分かっている
- **物理は機械ごとに答えが少しずれる**ので、通信で位置を合わせにくい
- 壁で止まる・坂を登るは CharacterController がやってくれる

代わりに、**アクセルで速さが増え、ハンドルで向きが変わる**という単純な計算にしてある。

### 通信のための備え

**まだ通信は入っていない。** ただし、あとで入れやすい形にしてある。

| やっていること | なぜ |
| --- | --- |
| **周回を数えるのは `RaceManager` の1か所だけ** | **ホストの上だけで動かせば**、そのまま公平な判定になる |
| カート側は順位も周回も持っていない | 配る情報が「段階」と「周回数」だけで済む |
| 走っている人は**自分から名乗り出る**（`RaceRacer.All`） | 途中から参加した人も、探しにいかずに拾える |
| スタート位置を**4つ**用意してある | 人数が増えても並べ直すだけで済む |
| カメラとHUDは**「自分のカート」を選んで映す** | 通信で自分の1台だけ映せばよい |

---

## できていないこと・既知の問題

- **まだ実際に走らせて確かめていない。** 速さ・ハンドルの効き・カメラの距離は**調整前の値**
- **通信（マルチプレイ）は入っていない。** いまは1台だけ
- **コンピューターの相手（AI）がいない。** 順位は必ず1位になる
- **アイテム・加速パネル・ドリフトは無い**
- **コースは仮のもの。** 楕円を1つ置いただけで、高低差もショートカットも無い
- **落ちたときの復帰が無い。** 壁で囲ってあるので落ちないはずだが、
  壁を越えてしまったら **R** で戻すしかない
- **逆走の警告が無い。** 逆走しても止められないが、**周回にも数えられない**
- **音が鳴らない。** エンジン音もカウントダウンの音も無い
- **順位はゴールした順だけ。** 走っている最中の「今何位か」は出していない
  （1人しかいないため。通信を入れるときに足すこと）
- **タイヤは回らない。** 見た目だけの飾り

---

## 作り直したいとき

**`Tools > Mirai01 > レースの検証シーンを作り直す`**

`RaceKart.prefab` と `RaceTest.unity` が**上書きで作り直される。**
**手で調整した内容は消える**ので、実行する前に確認すること。

---

## 変更ログ

| 日付 | 変更者 | 内容 |
| --- | --- | --- |
| 2026/9/10 | Claude Code | 新規作成。カウントダウン・1周でゴール・タイム・やり直しまで |
| 2026/9/10 | Claude Code | **ロボットが走る版のシーンを追加**したのに合わせて、共通の作りを少し広げた。①位置を見る先を差し替えられるようにした（体が動いて入れ物が動かない作りに対応）②やり直しのやり方を差し替えられるようにした③速度を**位置の変化から**出すようにして、乗り物以外でも出せるようにした④画面左下の操作案内を、シーンごとに変えられるようにした。**カートのレースの動きは変わらない**。詳細は `ロボットが走るレース.md` |
