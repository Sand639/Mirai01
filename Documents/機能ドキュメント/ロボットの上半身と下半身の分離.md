# ロボットの上半身と下半身の分離

| 項目 | 内容 |
| --- | --- |
| 担当者 | Claude Code（依頼：大槻 海斗） |
| 作成日 | 2026年9月2日（水） |
| 最終更新日 | 2026年9月4日（金） |
| 状態 | 完成（プロトタイプとして） |

---

## この機能は何か

**ロボットの体を上半身と下半身に切り離して、それぞれを別々に操作できる機能。**

合体ロボのような遊びを試すためのプロトタイプ。
「上半身だけで狭い所に入る」「下半身だけを先に行かせる」といった遊びを想定している。

**カメラは操作している方を追いかける。** 切り替えると滑らかにそちらへ移る。

---

## 遊ぶ人から見た動き

| 操作 | 結果 |
| --- | --- |
| **W / A / S / D** | いま操作している方を動かす |
| **Space** | **ジャンプする**（合体中と、分離中の下半身だけ） |
| **F** | **目の前の物を持つ／離す**（合体中と、分離中の上半身だけ） |
| **V** | **一人称と三人称を切り替える** |
| **マウス** | カメラを回す（**操作している方を追いかける**） |
| **Q** | **上半身と下半身を切り離す** |
| **Tab** | **操作する方を切り替える**（上半身 ⇔ 下半身） |
| **E** | 近づいていれば**合体する** |
| **Escape** | マウスカーソルを出す（クリックで操作に戻る） |

- **ジャンプできるのは、合体しているときと、分離中の下半身だけ。**
  上半身だけのときは跳べない（脚が無いため）
- **物を持てるのは、合体しているときと、分離中の上半身だけ。**
  下半身だけのときは持てない（腕が無いため）。**ジャンプとちょうど逆**

### 画面中央のレティクル（照準）

**画面の真ん中に、小さな十字が出る。**
プレイヤーのプロトタイプと同じもの。

**持てる物を狙えているときは、色が水色に変わって少し大きくなる。**
一人称ではマウスカーソルが消えているので、これが無いと狙いが分からない。

### 一人称と三人称（V キー）

| | 見え方 | 向いている場面 |
| --- | --- | --- |
| **三人称**（最初はこちら） | 体の後ろからカメラが見る | 全体が見えて動かしやすい |
| **一人称** | 体の目の位置から見る | **狙いやすい。隙間を通るときに分かりやすい** |

- **一人称のときは、操作している体の見た目が消える。**
  そうしないと、目の位置から自分の体の内側が見えてしまう
- **操作していない体は消えない。** 分離中に相手を見失わないようにするため
- **目の高さは体ごとに違う。** 合体時は1.82m、上半身だけだと0.72m。
  上半身だけのときは、視点がぐっと低くなる

### 動かし方はフォートナイト型

**一人称でも三人称でも、体は常に「カメラの向いている方向」を向く。**
進む方向へ向き直る作りではない。

そのぶん、**進む向きによって速さが変わる。**

| 進む向き | 速さ | 意味 |
| --- | --- | --- |
| **前** | **そのまま**（100%） | 一番速い |
| **横**（左右キー） | **50%** | 前と比べて50%減 |
| **後ろ** | **70%** | 前と比べて30%減 |

斜めのときは、前後と左右の割合で混ぜる（「斜め前」なら前と横の中間）。

> **向きが固定されているからこそ、この差が付けられる。**
> 進む方向へ向き直る作りだと、常に「前」に進んでいることになるので差が出ない。

**元の作り（進む方向へ向き直る）に戻したいときは、**
`RobotRig > Robot View Switcher > Face Look Direction` を OFF にする。

### 物の持ち方

| | |
| --- | --- |
| 狙う | **持てる物に近づくと、その物の色が水色に変わる**（狙えている合図） |
| 持つ | **F** を押す。手の前に浮いた状態になる |
| 離す | **もう一度 F** を押す。その場に落ちる |

- 届く範囲は**2m以内で、正面から70度以内**
- **持ったまま切り替えても、持ち続ける。**
  上半身に持たせたまま、下半身を操作しに行ける
- **合体・分離で体が入れ替わったときは、持ち替える。**
  持てる体がいなくなった場合だけ、その場に落ちる
- **離せるのは、持っている本人だけ。**
  上半身が持っている物を、下半身を操作しながら離すことはできない
- 切り離すと、**上半身が少し後ろに離れて**現れる
- **操作していない方はその場で止まる。** 勝手に動き回らない（重力では落ちる）
- 合体できるのは、2つが**2.5m以内に近づいている**とき（高さの差は見ない）
- 合体すると、**下半身のいた場所に合体した姿が現れる**

### 大事な作り

**合体しているときは1体、分けたときは2体**という作りにしている。

2つの体を物理的に繋いだまま動かすとガタつくため、**繋ぐのをやめて
「合体した姿の体」と「上半身」「下半身」を入れ替える**方式にした。
見た目は繋がったり分かれたりしているが、中身は入れ替わっている。

### 3つの状態

| 状態 | 意味 |
| --- | --- |
| `Combined` | 合体中。1体として動く |
| `SplitUpper` | 分離中。上半身を操作している |
| `SplitLower` | 分離中。下半身を操作している |

---

## すぐ試す方法

1. Unityで `Assets/Scenes/Test/RobotSplitTest.unity` を開く
2. **再生ボタンを押す**

シーンには次のものが置いてある。

- **低い隙間**（正面）… 合体したままでは通れず、**上半身だけなら通れる**
- **低い段差**と**高い段差**（左右）… 登れる高さの違いを試せる
- **目印の箱**（奥）… 分かれたまま歩き回るときの目安
- **紫の箱3つ**（手前）… **持てる箱**。大きさと重さが違う

「上半身だけで箱を持って、隙間を抜けて運ぶ」といった動きも試せる。

「上半身だけで隙間を抜けて、下半身を呼び戻して合体する」といった動きを試せる。

---

## 関係するファイル・シーン

| 種類 | パス |
| --- | --- |
| スクリプト | `Assets/Scripts/Robot/RobotController.cs`（分離・合体・切り替え・移動） |
| スクリプト | `Assets/Scripts/Robot/RobotBody.cs`（体1つ分の動き） |
| スクリプト | `Assets/Scripts/Robot/RobotCameraLook.cs`（カメラの向きと追いかけ） |
| スクリプト | `Assets/Scripts/Robot/RobotGrabber.cs`（物を持つ／離す） |
| スクリプト | `Assets/Scripts/Robot/Grabbable.cs`（持てる物に付ける印） |
| スクリプト | `Assets/Scripts/Robot/RobotViewSwitcher.cs`（一人称・三人称の切り替え） |
| スクリプト | `Assets/Scripts/Player/Reticle.cs`（**プレイヤーと共通**。画面中央の照準） |
| スクリプト（エディタ用） | `Assets/Scripts/Player/Editor/ReticleFactory.cs`（**プレイヤーと共通**。レティクルを作る道具） |
| スクリプト（エディタ用） | `Assets/Scripts/Robot/Editor/RobotRigSetup.cs`（シーン生成ツール） |
| プレハブ | `Assets/Prefabs/RobotRig.prefab` |
| シーン | `Assets/Scenes/Test/RobotSplitTest.unity` |
| マテリアル | `Assets/Art/Materials/RobotUpper.mat` ほか2つ |
| 入力設定 | `Assets/InputSystem_Actions.inputactions`（**既存のものを使用。変更していない**） |

> シーン（`.unity`）とプレハブ（`.prefab`）は**同時に2人以上で編集できない**。
> 触るときは担当者に一声かけること。

### プレハブの中身

```
RobotRig                    ← RobotController（まとめ役）／ RobotGrabber（物を持つ）
│                              ／ RobotViewSwitcher（一人称・三人称）
├── ReticleCanvas           ← 画面中央の照準
│   └── Reticle
├── CameraRig               ← RobotCameraLook（左右の向き＋追いかけ）
│   └── CameraPivot         ← 上下の首振り
│       └── RobotCamera     ← カメラ
├── CombinedBody            ← 合体した姿。CharacterController / RobotBody
│   ├── LowerVisual         ← 下半身の見た目
│   ├── UpperVisual         ← 上半身の見た目
│   └── HoldPoint           ← 持った物を置く場所
├── UpperBody               ← 分けたときの上半身
│   ├── Visual
│   └── HoldPoint           ← 持った物を置く場所
└── LowerBody               ← 分けたときの下半身
    └── Visual              ← **HoldPoint は無い（＝物を持てない）**
```

> **「下半身は物を持てない」は、`HoldPoint` が入っていないことで決まっている。**
> プログラムの中で状態を見て分岐しているのではない。
> **持てるようにしたければ、`HoldPoint` を作って `Robot Body > Hold Point` に入れるだけでよい。**

**3つの体のうち、いつも1つか2つだけが出ている。**

| 状態 | 出ている体 |
| --- | --- |
| 合体中 | `CombinedBody` だけ |
| 分離中 | `UpperBody` と `LowerBody` |

**カメラは体の子にしていない。** 体の子にすると、体が動くたびに引っ張られて
見た目がカクつくため、毎フレーム体の位置へ寄せていく形にしている。

---

## 設定できる値（インスペクターの項目）

シーンの `RobotRig` を選ぶと、`Robot Controller` が出てくる。

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Combine Distance** | **この距離まで近づくと合体できる（m）** | 2.5 |
| **Upper Split Offset** | 切り離したとき、上半身が現れる位置（足元から見て） | (0, 1.0, -0.9) |
| Lower Split Offset | 切り離したとき、下半身が現れる位置 | (0, 0, 0) |
| **Camera Always On Upper** | **ONだと下半身操作中もカメラは上半身のまま。OFFで操作中の方を追う** | OFF |
| **Upper Can Jump** | **ONにすると、分離中の上半身もジャンプできるようになる** | OFF |
| Split / Combine / Switch Key | 各操作のキー | Q / E / Tab |

**進む速さは体ごとに設定する。** `CombinedBody` / `UpperBody` / `LowerBody` を選ぶと
`Robot Body` が出てくる。ここで**上半身だけ遅くする**といった差を付けられる。

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Move Speed | 進む速さ（1秒あたりのメートル） | 5 |
| Turn Speed | 進む方向へ向き直る速さ（1秒あたりの角度） | 720 |
| Gravity | 落ちる強さ。**マイナスの値にすること** | -20 |
| **Side Speed Rate** | **横に動くときの速さ**（前を1としたときの割合） | 下の表を参照 |
| **Back Speed Rate** | **後ろに動くときの速さ**（前を1としたときの割合） | 下の表を参照 |
| **Jump Height** | **ジャンプで上がる高さ（m）。0にすると跳ねなくなる** | 1.2 |
| **Hold Point** | **持った物を置く場所。空欄だとその体は物を持てない** | 上半身と合体時のみ設定 |
| **Eye Height** | **一人称のときのカメラの高さ（足元から）** | 合体1.82／上0.72／下0.88 |

**速さの割合として、いま入っている値（2026/9/4・大槻さんが調整済み）**

| 体 | 横 | 後ろ |
| --- | --- | --- |
| **CombinedBody**（合体時） | **0.7** | **0.5** |
| UpperBody（上半身） | 0.5 | 0.7 |
| LowerBody（下半身） | 0.5 | 0.7 |

> ⚠️ **合体時だけ、横と後ろが他の2つと逆になっている。これは間違いではない。**
> **遊んでみて調整した結果**なので、「揃っていないから直そう」としないこと。

### Robot View Switcher（`RobotRig` を選ぶと出てくる）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Third Person Offset** | 三人称のとき、体からどれだけ後ろ・上に引くか | (0, 0.8, -5.5) |
| Third Person Height | 三人称のとき、追いかける高さ | 1.4 |
| **First Person Forward** | 一人称のとき、目の位置からどれだけ前に出すか | 0.25 |
| Switch Key | 視点を切り替えるキー | V |
| Switch Smooth | 切り替わるときの滑らかさ。0で一瞬 | 12 |
| Start In First Person | ONだと、最初から一人称で始まる | OFF |
| **Face Look Direction** | **ONだと、一人称でも三人称でも体がカメラの向きを向く**（フォートナイト型）。OFFにすると、進む方向へ向き直る昔の作りに戻る | ON |

### Robot Grabber（`RobotRig` を選ぶと出てくる）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| **Reach** | **この距離まで近づくと持てる（m）** | 2 |
| **Max Angle** | 正面からどれだけ横にずれていても持てるか（度） | 70 |
| Grab Key | 持つ／離すを切り替えるキー | F |
| Highlight Color | 狙えている物の色 | 水色 |
| **Release Push** | **離すときに前へ押し出す強さ。0ならその場に落とす** | 0 |

### Grabbable（持てる箱を選ぶと出てくる）

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Hold Offset | 手の位置から、どれだけずらして持つか | (0,0,0) |
| Straighten When Held | ONだと、持ったときに向きをまっすぐ揃える | ON |
| Can Be Carried | OFFにすると、**持てない物**にできる（重すぎる物を表現したいとき） | ON |

> **上半身が跳べないのは `Robot Controller > Upper Can Jump` が OFF だから**で、
> `Jump Height` を 0 にしているわけではない。ONにすれば上半身もこの高さで跳ぶ。

`RobotCameraLook`（カメラ）側：

| 項目名 | 意味 | 初期値 |
| --- | --- | --- |
| Sensitivity | マウスで回る量 | 0.12 |
| Max Look Up / Down | 上下を向ける限界の角度 | 70 / 70 |
| Target Height | 体の足元から、どれだけ上を見るか（m） | 1.4 |
| Follow Smooth | 追いつく速さ。大きいほどぴったり付いてくる | 14 |

---

## 仕組み（分かる人向け）

- 状態は **enum（`Combined` / `SplitUpper` / `SplitLower`）** で管理している（依頼時の設計どおり）
- **体を繋いでいない。** 3つの体を `SetActive` で出し入れして入れ替えている
- 切り離すときは、合体した体の**足元を基準**に上半身と下半身を置く。
  上半身は少し後ろに出すので、下半身の上に乗って動けなくなることがない
- 合体するときは、**下半身のいた場所**に合体した体を置く。向きは操作していた方に合わせる
- 動きは **CharacterController**。物理演算で押し合わないので、指示したとおりに動く
- 位置を変えるときは、CharacterController を**いったん切ってから**動かしている。
  動いている最中に位置を変えると暴れるため
- カメラは `LateUpdate` で体の位置へ寄せている。体を動かしたあとに動かす必要があるため
- ジャンプは、**指定した高さまで上がる速さを逆算**して入れている
  （`Mathf.Sqrt(高さ * -2 * 重力)`）。プレイヤーの操作キャラと同じやり方
- **誰が跳べるかは `RobotController` が決めている。** 体の側（`RobotBody`）は
  「跳べと言われたら、地面に付いていれば跳ぶ」だけにしてある。
  上半身を跳べるようにしたくなっても、体の側を触らずに済む

### 参考コードから変えたところ

依頼時の設計（enum管理・分離と合体・非操作側の停止）は踏襲しつつ、
**実際に動かしたときの不具合を受けて、繋ぎ方と動かし方を変えた。**

| 変えた点 | 理由 |
| --- | --- |
| **Jointをやめて、1体/2体の入れ替えにした** | **合体中にガクガクしていたため。** Jointは2つを引っ張り合って位置を保つ仕組みで、そこへ毎フレーム速度を直接入れると計算がぶつかる |
| **Rigidbody → CharacterController** | 同上。物理で押し合わないので、揺れず確実に動く |
| **カメラを体の子から外して追従にした** | **視点が滑らかでなかったため。** 物理は固定間隔、描画は可変間隔で動くので、体の子だとズレが見える |
| **切り離す位置を離した** | **分割時にWASDで動かなかったため。** 上下の当たり判定が接していて押し合っていた |
| `Input.GetKeyDown` / `Input.GetAxis` → **Input System** | このプロジェクトは Input System に統一する決まりのため |
| 移動を **カメラの向き基準**にした | 参考コードは体の向き基準で、左右キーが回転も兼ねていたため操作しづらかった |
| 進む速さを**体ごとに持たせた** | 「上半身は遅い」といった差を後から付けられるようにするため |

### 拡張ポイント

分離・合体・切り替えのタイミングで**イベントが呼ばれる**ようにしてある。
エフェクトや音を足したいときは、これを購読すればよい（`RobotController.cs` にコメントあり）。

| イベント | いつ呼ばれるか |
| --- | --- |
| `Split` | 切り離した瞬間 |
| `Combined` | 合体した瞬間 |
| `ControlSwitched` | 操作する方が切り替わったとき |

`State`（いまの状態）と `CanCombine`（いま合体できるか）も公開してあるので、UI表示に使える。

---

## できていないこと・既知の問題

- **動作確認はUnityの再生で行っていない。** コンパイルが通ることと、
  参照が正しくつながっていることまでは確認済み。**実際の操作感は未確認**
- **物理で押されない。** CharacterController にしたため、他の物にぶつかられても動かないし、
  箱を押しのけることもできない。揺れないことと引き換えの割り切り
- **切り離した瞬間の演出が無い。** 上半身が跳ねて離れるような動きは付けていない
  （物理を使わなくなったため）。必要なら見た目だけを動かす形で足せる
- **フォートナイト型の動かし方は、まだ再生して確かめていない。**
  **横歩き・後ろ歩きの速さの差が、遊んでみて自然かは要確認**。
  速すぎる／遅すぎると感じたら、`Side Speed Rate` と `Back Speed Rate` で調整する
- **三人称のカメラは体の真後ろにある。** フォートナイトのような
  「肩越し（少し右にずらす）」にはしていない。
  ずらすとレティクルの位置と実際に狙える方向がずれるため、まず真後ろで作ってある。
  **肩越しにしたい場合は、狙いの補正も一緒に考える必要がある**
- **一人称のとき、腕は出ない。** プレイヤーのプロトタイプのような
  「画面手前の腕」は付けていない
- ~~持ち上げは未確認~~ → **2026/9/4に大槻さんが確認済み。持てている**
- ~~一人称とレティクルは未確認~~ → **2026/9/4に大槻さんが確認済み**
- **持った物は壁をすり抜ける。** 持っている間は物理を止めているため。
  持ったまま壁に近づくと、箱が壁の中に入る。**プロトタイプでは困らないと判断した**
- **持った物と自分の体はぶつからないようにしてある。** そうしないと、
  自分の持った箱に押されて動けなくなるため
- **持てるのは1つだけ。** 両手で2つ持つことはできない
- **ジャンプは1回だけ。** 空中でもう一度押しても跳べない（二段ジャンプは無い）。
  また、**ジャンプの高さや操作感は再生して確かめていない**ので、
  低い／高いと感じたら `Jump Height` で調整すること
- **ジャンプで届く場所を試す足場が、検証シーンに無い。** 段差はあるが、
  「ジャンプしないと登れない高さ」の台は置いていない
- **上半身だけでも普通に歩ける。** 「脚が無いから遅い」といった差は付けていない。
  各体の `Robot Body > Move Speed` を変えれば差を付けられる
- **合体は距離だけで判定している。** 向きは見ていないので、
  下半身の上に乗っていなくても近ければ合体できてしまう
- **合体する場所に物があっても合体できる。** 壁の中に現れることがありうる
- **アニメーションは無い。** 箱が滑るように動く

---

## 作り直したいとき

**`Tools > Mirai01 > ロボット分離の検証シーンを作り直す`**

プレハブ・シーン・マテリアルがまとめて作り直される。
**手で調整した内容は消える**ので、実行前に確認すること。

> ⚠️ **いまは実行しないほうがよい。**
> `RobotRig.prefab` の速さの値は**手で調整したもの**なので、作り直すと初期値に戻る。
> どうしても作り直す場合は、**先に今の値を控えておくこと**（上の表）。

---

## ステージを作るとき

**`RobotSplitTest.unity` の上にステージを作らないこと。**
このシーンは上のツールで**まるごと作り直される**ため、作ったものが消える。

**新しいシーンを作って、そこに `RobotRig.prefab` を置く。**

| やること | 内容 |
| --- | --- |
| 置き場所 | `Assets/Scenes/Test/`（まだ企画が決まっていないため） |
| 名前 | 半角英数字。例：`RobotStage01` |
| 置くもの | **`Assets/Prefabs/RobotRig.prefab` を1つ置くだけ**で動く |
| 消すもの | **シーンに元からある `Main Camera` を消す**（プレハブがカメラを持っているため、二重になる） |
| 明かり | `Directional Light` は残しておく |

> **シーンは同時に2人以上で編集できない。**（自動マージできない設定のため）
> 作り始める前に、Discordで一声かけること。

---

## 変更ログ

| 日付 | 変更者 | 内容 |
| --- | --- | --- |
| 2026/9/2 | Claude Code | 新規作成。分離・合体・操作切り替え・カメラ追従まで |
| 2026/9/2 | Claude Code | **1体/2体の入れ替え方式に作り直した**（Jointをやめ、CharacterControllerに変更、カメラを追従に変更）。合体時のガクガク・視点のカクつき・分割時に動かない問題を解消 |
| 2026/9/3 | Claude Code | **ジャンプを追加**（Space）。合体中と分離中の下半身だけが跳べる。上半身は `Upper Can Jump` を ON にすれば跳べる |
| 2026/9/4 | Claude Code | **物を持てるようにした**（F で持つ／離す）。**合体中と分離中の上半身だけ**が持てる（ジャンプとちょうど逆）。持てるかどうかは、体に「手の位置」が入っているかで決まる。検証シーンに持てる箱を3つ追加 |
| 2026/9/4 | Claude Code | **一人称・三人称の切り替え（V）と、画面中央のレティクルを追加。** レティクルはプレイヤーのプロトタイプと同じものを、共通の道具（`ReticleFactory`）にまとめて両方から使うようにした。一人称のときは、操作している体の見た目だけを隠す |
| 2026/9/4 | Claude Code | **一人称のとき、体が「見ている方向」を向くようにした**（大槻さんの依頼）。三人称は今までどおり進む方向へ向き直る。`Face Look Direction In First Person` を OFF にすれば元に戻せる |
| 2026/9/4 | Claude Code | **動かし方をフォートナイト型にした**（大槻さんの依頼）。**一人称でも三人称でも体はカメラの向きを向き**、進む向きで速さが変わる（前100%／横50%／後ろ70%）。`Face Look Direction` を OFF にすれば元に戻せる |
| 2026/9/4 | Claude Code | **速さの値は調整済みであることを明記**（合体時だけ横と後ろが逆なのは意図的）。あわせて「ステージを作るとき」の項目を追加（検証シーンの上に作らないこと） |
